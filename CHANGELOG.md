# 변경 기록

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은
[유의적 버전](https://semver.org/lang/ko/)을 따른다.

이 저장소에서 공개 표면은 화면의 다이얼과 주소 형식이다. 주소에 담긴 설정으로 남긴
링크가 나중에 다른 장을 찍으면 그것은 깨진 것이므로, 같은 롤이 같은 장을 찍지 않게 되는
변경은 MAJOR로 올린다.

## [0.37.0] - 2026-09-18

METEOR의 머리를 다시 짰다. 두 가지다.

하나는 법선이다. 머리 원점(HEAD 0)을 지나 축에 수직인 선 — 머리의 법선 — 을 넘어 앞으로 튀는 것이
이제 없다. 머리에 딸린 것은 모두 꼬리 쪽 반평면, 축에서 양쪽으로 90°씩 모두 180° 안에 있다.
이펙트 작화로 치면 방출 원뿔의 반각이 90°인 반구 이미터다.

다른 하나는 방출이다. 머리의 조각들은 머리에 박힌 채 저마다 다른 박자·방향·색으로 깜빡여, 장마다
윤곽이 제멋대로 바뀌었다. 이제 조각은 머리에서 태어나 날아가고, 부채꼴은 고른 빗살이며, 색은
나이를 따르고, 머리 전체가 박자에 맞춰 두 자세를 오간다.

같은 롤의 METEOR가 0.36.0과 다른 장을 찍는다. 다른 판은 그대로다.

### 바꿈

- **결정 조각(SHARDS)은 뿜어 나간다.** 머리 원점에서 태어나 제 빗살을 따라 날아가다 느려지며 꼬리
  쪽으로 처져 휘고, 작아져 사라진다. 한 바퀴에 2 × SPEED번 태어나 루프가 닫힌다
  - 빗살: SPREAD를 반각으로 한 부채꼴을 조각 수만큼 고르게 나누고 칸 안에서 조금 흔든다. 가운데가
    길고 멀리 가며 바깥으로 갈수록 짧다
  - 태어나는 때는 황금비로 흩는다. 흐름이 고르고, 이웃한 빗살이 함께 태어나지 않는다
  - 색은 나이를 따른다. 갓 태어나면 희고, 노랗다가, 끝에서 분홍으로 식는다. 여섯에 하나쯤은 내내
    푸른 강조다
  - 기본 10개에서 12개로 늘렸다. 한 번에 보이는 수다
- **갈고리 가시(BARBS)는 좌우 대칭 쌍이다.** 같은 모양이 양쪽으로 하나씩, 안쪽 쌍이 길고 바깥 쌍일수록
  짧고 더 벌어진다. 가늘고 짧게 줄여 축을 가리지 않는다. 기본 넷에서 한 쌍(둘)으로 줄이고 둘씩
  오른다
- **큰 결정(SPIKE)은 가는 V다.** 꼭짓점이 머리 원점에 닿고, 속을 깊이 파서 두 날만 뒤로 벌어진다.
  통째로 크면 매듭과 조각을 삼켜 머리가 한 덩어리가 됐다
- **머리의 두 자세.** 조각마다 따로 흔들던 깜빡임을 걷고, 박자마다 두 자세를 번갈아 오간다(FLICKER).
  투스로 끓이는 이펙트 작화의 방식이다
- **법선.** 조각과 가시는 윤곽 전체가 법선 안에 들 때까지 기울기를 좁힌다(`withinNormal`). 갓 태어나
  꼬리가 법선 앞으로 나온 조각은 그만큼 뒤로 민다. 매듭은 분홍 심의 앞끝이 법선에 닿도록 뒤로
  물렸고, 불티는 법선에서 태어나 꼬리 쪽으로 튄다
- **임팩트 프레임.** 네 갈래 반짝임 대신 세 갈래로 터진다 — 꼬리 쪽으로 길게 하나, 법선을 따라 짧게
  둘. 앞은 법선에서 곧게 끊긴다. 옆 갈래까지 길면 법선을 따라 흰 벽이 서서 짧게 뒀다. 둥근 섬광도
  앞끝이 법선에 닿게 물렸다
- 안내선이 법선을 긋는다(NORMAL · 180°). MAP.md에 반구 방출 · 방출 원뿔을 더했다

### 확인한 것

- 주변 흐름(티끌 · 연기 · 속도선 · 파편 · 별 · 보조 광선 · 번짐)을 끄고 머리의 손잡이를 끝까지 올린
  장(SPIKE 1.6, BARBS 8, SHARDS 24, SPREAD 1.4, FLICKER 1, GRAIN 0, REGISTER 0)을 9픽셀 칸으로
  나눠, 하늘보다 밝은 칸이 법선 앞(u < −10 px)에 있는지 셌다. 임팩트 프레임 둘을 포함한 프레임 열에서
  모두 0칸이다. 가장 앞의 밝은 칸은 −0.7 px로, 피벗이 든 칸이다. 같은 잣대로 0.36.0의 METEOR는
  267칸(가장 앞 −197 px), 섬광 때는 375칸(−208 px)이 법선 앞에 있었다
- 조각의 나이와 머리의 자세는 장의 시간(held)으로만 정하고, 한 바퀴에 정수 번 돈다. t=1은 t=0과
  같은 장이다
- 배색 셋(AQUA × GOLD × PINK, GOLD × SKY × MIST, 통 둘의 LEMON × AQUA)에서 머리와 뿜어 나가는
  조각이 읽힌다. 안내선을 켜고 프레임을 넘겨도 에러가 없다
- 고친 것은 METEOR 한 파일이다. 다른 판은 이 파일을 부르지 않는다

## [0.36.0] - 2026-09-18

별똥별 판 METEOR를 걸었다. 받은 이펙트 분해 장면처럼 이미터를 층층이 쌓아 그린다 — 연기, 보조 광선,
주 광선, 속도선, 파편, 별, 불티, 큰 가시, 갈고리 가시, 결정 조각, 머리 매듭. 애니메이션 이펙트 작화의
문법 — 또박또박한 박자, 납작한 면, 각진 파편, 임팩트 프레임 — 을 따른다. 다른 판은 그대로다.

그리고 프로젝트를 한 번 훑어 정리했다. 판마다 따로 쓰던 도구를 모으고, main.js와 인쇄기를
나눴다. 고치기 전에 모든 판을 520가지로 찍어 해시를 떠 두고(스냅숏), 단계마다 다시 떠서 한 픽셀도
바뀌지 않았음을 확인했다. 임팩트 프레임을 고친 METEOR의 두 장만 다르다.

### 더함

- **METEOR.** 하늘은 가장 노란 통 하나만 빼고 모든 통(밤)으로 깔고, 빛나는 것은 파낸다. 노란 빛은
  밤을 다 파내고 노란 통을 얹고, 푸른 빛과 분홍 빛은 가장 푸른 통과 가장 붉은 통을 조금 남기고,
  흰 빛은 노란 통까지 파내고, 어둠은 밤을 하늘보다 진하게 찍고 노란 통을 파낸다. 하늘에는 노란
  통을 옅게 깔아 푸르스름한 회색을 낸다
  - 광선: 머리에서 꼬리 끝까지 뻗는 넓은 노란 띠에 흰 심과 꼬리 끝의 푸른 면. 조금 다른 각도로 뻗는
    분홍·푸른 보조 광선(BEAMS). 둘레에 흐린 번짐 한 겹
  - 연기(SMOKE): 축을 따라 길게 늘인 어두운 얼룩이 광선 위를 덮으며 흘러 깊이를 만든다. 가장 진한
    통만 옅게 얹어 검정이 아니라 짙은 남보라다
  - 머리: 앞으로 솟은 큰 결정 하나(SPIKE), 안쪽이 오목하게 휜 흰 갈고리 가시(BARBS), 부채꼴로 돋는
    작은 조각(SHARDS), 짧고 밝은 불티, 그리고 가장 앞의 분홍 심과 노란 타원으로 된 매듭
  - 머리에 딸린 것은 모두 머리 원점(HEAD 0), 광선이 시작하는 자리를 붙박이로 두고 늘고 준다. 임팩트
    프레임의 흰빛도 여기서 터진다. 광선은 늘고 줄지 않으므로, 다른 점을 잡고 키우면 머리가 광선
    위에서 앞뒤로 미끄러진다
  - 둘레: 어두운 바늘 속도선(셋에 하나는 앞이 굵은 화살), 각진 파편(CHIPS), 점선 꼬리를 단 별(SPARKS),
    먼 하늘의 티끌
  - 움직임: 별똥별은 제자리에 있고 둘레가 꼬리 쪽으로 흐른다(SPEED, 한 바퀴에 정수 번). 흐르는 것은
    양 끝에서 가늘어져 되감기는 자리가 보이지 않는다. 조각은 저마다 돋았다 사그라들고(FLICKER), 몸은
    부풀었다 가라앉는다
  - **또박또박한 박자.** 한 바퀴를 BEAT장으로 나누고 그 장의 시간으로만 그린다. 사이를 메우지 않아
    모양이 장마다 뚝 바뀐다. 기본 12면 네 프레임에 한 장이고, 48이면 프레임마다 다시 그려 부드러워진다
  - **납작한 면.** 꼬리는 계조가 아니라 납작한 면 셋이다 — 노란 몸, 그 안의 흰 속, 꼬리 끝의 푸른 면.
    가장자리는 마디가 적어 직선으로 꺾인다. 번짐은 한 겹만 깔아 붙인 색종이로 보이지 않게 한다
  - **각진 파편(CHIPS).** 둥근 방울 대신 모서리가 선 조각이 돌면서 흘러간다. 넷에 하나는 푸른 빛이다
  - **임팩트 프레임(FLASH).** 한 바퀴에 한 번, 두 프레임 동안 머리에서 흰빛이 터지고 분홍 조각 말고는
    모두 희어지고 하늘까지 옅어진다. 시작은 6의 배수 프레임이라 초당 24·12·8장 어느 박자로 찍어도
    빠지지 않는다. 이음매에서는 멀리 떨어뜨려 이음매의 걸음에 끼지 않게 한다
  - 손잡이는 SKY(FIELD·DARK·DUST), BEAM(ANGLE·LENGTH·CORE·BEAMS·GLOW), HEAD(SPIKE·BARBS·SHARDS·
    SPREAD·FLICKER), FLOW(SMOKE·STREAKS·CHIPS·SPARKS·SPEED), BEAT(BEAT·FLASH)로 묶는다. 무엇이든
    끝값만큼 늘 뽑고 손잡이는 몇을 찍을지만 정한다
- **src/blur.js.** JELLY의 상자 흐림과 종 둘레의 빛을 공용으로 옮겼다. METEOR가 꼬리와 머리의
  번짐에 쓰고, 롤과 모양 손잡이가 같으면 다시 짓지 않고 쥐고 있는다
- **src/drums.js.** 가장 붉은 통을 고르는 reddest를 더했다
- **GUIDES.** 판이 제 뼈대를 안내선으로 내놓을 수 있다. 판에 `guides(page)`가 있으면 찍은 바로 뒤에
  그 판이 쓴 page로 부르고, 돌려준 표시를 종이 위에 겹친 다른 캔버스에 그린다. 인쇄된 픽셀은
  건드리지 않으므로 PNG와 콘택트 시트는 그대로다. 단추와 D 키로 켜고 주소에도 실린다(guides=1).
  자리는 종이의 칸이고 굵기와 글자는 화면의 픽셀이라, 종이가 작게 걸려도 가늘어지지 않는다
  - METEOR는 축(흐르는 것이 도는 구간), 머리 원점, 주 광선의 윤곽, 지금 몇 번째 장인지와 임팩트
    프레임이 언제 서는지를 짚는다. 머리 원점은 머리에 딸린 것들의 피벗이라 멈춘 장 하나만 봐서는
    잡히지 않는다
  - 안내선을 내놓지 않는 판은 종이의 것을 받는다 — 여백과 한가운데
  - 인쇄기는 찍은 page를 함께 돌려준다. 안내선이 판과 같은 값을 보게 하기 위한 것이다

### 바꿈

판화와 인쇄기의 안쪽이다. 찍히는 장과 주소 형식은 그대로다.

- **어두운 시야의 도구(src/night.js).** 밤의 통을 깔고(floodNight), 빛을 파내고(carve), 어둠을
  얹는(stain) 고리를 CELL, COSMOS, FLAKE, METEOR가 저마다 손으로 쓰고 있었다. 한 곳으로 모았다.
  판마다 통을 얼마나 남기는지는 그대로 판이 정한다. METEOR의 밤과 노란 빛 나누기는
  src/drums.js의 nightAndGlow로 옮겼다. nightAndLight와 합치지 않은 것은, 합치면 배색 셋에서 밤이
  달라지기 때문이다
- **쥐고 있기(src/keep.js).** 짠 조직, 구운 성운, 흐린 무늬를 쥐는 캐시가 다섯 벌 복사돼 있었다.
  keeper(limit) 하나로 모았다. METEOR가 흐린 무늬를 제 캔버스로 옮기던 일은 blur.js의 keepGlow가 한다
- **FIELD 씨앗(rng.js의 fieldSeed).** 여섯 판에 같은 공식이 복사돼 있었다. JELLY는 곱수가 달라 제
  곱수를 넘기고, 공식이 다른 RIPPLE은 그대로 둔다. 공식을 바꾸면 저장된 주소가 다른 장을 찍는다
- **경로 도구(shapes.js).** 새 경로를 열지 않고 더하기만 하는 polySubpath, circleSubpath,
  sparkleSubpath와 넓이(signedArea)를 더하고, 판마다 복사된 것을 걷었다
- **main.js를 나눴다.** 790줄이 598줄이 됐다. 주소 읽기와 쓰기는 src/hash.js로(DOM에 기대지 않아
  `npm test`로 시험한다), 안내선은 src/guides.js로, 콘택트 시트는 src/contact.js로 옮겼다
- **인쇄기의 print를 둘로 나눴다.** 판을 짜는 compose와 GPU로 찍는 stamp다. print는 이제 page,
  sketch, passes만 돌려준다. 나머지는 아무도 읽지 않았고 page에 다 들어 있다
- **판화의 paint가 돌려준 것(sketch)이 안내선으로 간다.** guides(page, sketch). METEOR가 안내선에 넘길
  뼈대를 모듈 변수에 몰래 적어 두던 일이 없어졌다
- **안 쓰는 것을 걷었다.** strokePoints, scope의 specks, inkIndex, Separation의 context. 인쇄기의
  rgb는 drums.js의 rgbOf와 같아 palette.js로 옮겨 하나로 했다. Separation.text는 type.js의 measure로
  글자 폭을 잰다
- 낡은 설명을 고쳤다 — CHLORO 소개글(엽록체는 흐르지 않고 저마다 제자리에서 움직인다), scope.js와
  JELLY의 판 목록, MOON의 가려진 t와 아무 일도 않던 offset, README의 RIPPLE 물빛과 JELLY의 깊이와
  종의 통, 걸리지 않은 MEDIUM을 가리키던 문장

### 고침

- 주소를 손으로 고쳐 숫자 자리에 숫자가 아닌 것을 넣으면 화면이 멈췄다. `#p=x`는 배색을
  `PALETTES[NaN]`으로, `#cell=x`는 셰이더에 NaN을 넘겼다. 이제 숫자가 아니면 기본값으로, 범위를
  넘으면 끝값으로 앉히고, 배색 번호는 정수로 반올림한다. 제대로 된 주소는 예전과 똑같이 읽는다

### 확인한 것

- **스냅숏(bench/snapshot.html).** 판을 모두(걸리지 않은 POSTER · MEDIUM까지) 설정 520가지로 찍어
  장마다 픽셀의 해시를 남기고, 고친 뒤에 다시 떠서 견준다. 리팩터링이 그림을 바꾸지 않았음을
  증명하는 자리다. 기준은 `bench/results/2026-09-18-before-refactor/`
  - 임팩트 프레임의 시작을 6의 배수로 옮긴 뒤 다른 장은 METEOR의 두 장뿐이다. 섬광 창이 옮겨 간
    프레임 7이다. 다른 판은 모두 같다. 그 상태를 `bench/results/2026-09-18-after-fixes/`로 새로 떴다
  - 도구를 모으고, 어두운 시야를 나누고, main.js와 인쇄기를 나눈 뒤 세 번 모두 520장이 같다
  - 스냅숏이 작은 변화를 잡는지 따로 봤다. METEOR 하늘의 노란 통을 0.18에서 0.19로 바꾸면 노란
    통이 따로 서는 3통 장 29장만 다르고, 되돌리면 모두 같다
- 벤치를 판 열로 다시 돌렸다(`bench/results/2026-09-18-canvas2d-vs-webgl2-ten-plates/`). 한 장의 시간은
  장마다 한 픽셀을 읽어 GPU를 기다린 값이고, 셰이더는 GPU 타이머로 쟀다. 가장 무거운 판은 이제
  CHLORO(5.6 ms)가 아니라 JELLY(6.8 ms)다. 24fps의 한 프레임 예산 42 ms의 여섯에 하나쯤이다.
  METEOR는 2.5 ms다. README와 how.html의 표를 이 값으로 바꿨다
  - 이음매 z에서 CELL의 둘째 씨앗이 −3.9로 크게 음수인데, 옛 인쇄기도 같은 값이고 앞 실행과도
    소수점까지 같다. 이음매의 걸음이 평소보다 작은 것이지 튀는 것이 아니고, 씨앗마다 부호가 섞인다
- 주소는 테스트 여섯이 지킨다(`npm test`). 오늘 쓴 주소를 되읽기, 0.36.0 전의 주소, 시야가 k에 섞인
  예전 주소, 손으로 고친 숫자, 판에 없는 열쇠. 옛 main.js의 readHash에 제대로 된 주소 열 개를 넣어
  새 것과 견주면, 옛 것이 모르는 guides=1 하나 말고는 모두 같게 읽힌다
- JELLY는 흐림을 옮긴 뒤에도 0.35.0과 네 가지 설정(배색·통 수·FEATHER·다리 수를 달리한 것)에서
  픽셀까지 같다
- METEOR는 t=1로 찍은 장이 t=0과 씨앗 셋에서 픽셀까지 같다. 기본값, BEAT 4에 모든 층을 끝값에 둔
  설정, BEAT 48에 임팩트를 끈 설정 모두 같다. 여섯 씨앗의 이음매 z는 +0.24, −0.20, −0.28, −0.09,
  −0.07, −0.16으로 모두 ±2σ 안이고 부호가 섞인다 — 잡음이다
- METEOR에는 t=1−1e−9 검사가 맞지 않는다. 장으로 그리는 판이라 그 시간은 한 바퀴의 마지막 장이고
  첫 장이 아니다. 십만 바이트쯤 다른 것이 제 모습이다. 이런 판은 t=1과 t=0을 대신 본다(README)
- 손잡이를 모두 끝까지 올리거나 내려도, 몸과 번짐과 티끌을 빼도, 배색 넷(GOLD × SKY × MIST,
  LEMON × AQUA × CORAL, GOLD × ORANGE × SKY, AQUA × GOLD × PINK)의 통 하나·둘·셋 어디서도 깨지지
  않는다
- 한 장의 CPU 쪽 시간(GPU를 기다리지 않은 값, 세 번 잰 가운데 값)은 0.39 ms다. 번짐을 쥐고, 곡선
  대신 마디가 적은 다각형을 쓴다
- 이펙트 문법을 따르니 프레임 사이의 걸음이 0(같은 장을 쥐고 있음)에서 149.61(임팩트 프레임)까지
  벌어진다. 고르게 흐르던 예전 판은 이 폭이 훨씬 좁았다
- 안내선을 켠 장과 끈 장은 픽셀까지 같다. 같은 롤·같은 프레임에서 종이 캔버스의 해시가 같고,
  콘택트 시트를 펴면 안내선은 내려간다. 켜 두면 한 장에 0.3 ms쯤 더 드는데, 24fps의 41 ms 예산에
  견주면 없는 것과 같다

## [0.35.0] - 2026-09-17

JELLY의 머리와 다리를 받은 해파리 사진처럼 바꿨다. 머리는 작아지고 어깨가 둥글며, 꼭대기가 옅고
속이 비친다. 다리를 그리는 방식(굵기, 흰 속줄, 구완의 결과 가운데 선)은 그대로이고, 자리와 흐름만
바꿨다. 구완은 기본 다섯 개다. 다리의 난수를 무리의 흐름에서 떼어, 이제 TENTACLES와 ARMS를 바꿔도
해파리의 자리가 움직이지 않는다. 그 대가로 같은 롤과 FIELD가 0.34.x와 다른 자리에 해파리를 놓는다
(한 마리일 때는 그대로다).

### 바꿈

- **촉수.** 1.5배 길게 늘어진다. 파동은 긴 S자로 흐르고(말림을 0.7배로 줄이고, 흔들림이 끝으로
  덜 급하게 커진다), 끝으로 갈수록 바깥으로 벌어져 이웃끼리 엇갈린다
- **구완.** 종 속(종 높이의 35% 위)에서 시작해, 뿌리는 종에 가리고 밑단 아래로 흘러나온 것처럼
  보인다. 2.4배 길고, 가운데로 모여 서로 꼬이듯 엇갈리며 내려온다
- **머리의 생김새.** 반타원이던 지붕을 초타원(지수 2.5)으로 짚어 어깨가 둥글게 떨어지고 옆이 곧게
  내려온다. 높이는 반폭의 0.94배에서 1.08배로 높였다
- **머리의 크기.** 종을 BELL의 0.8배로 그린다. 다리 길이는 BELL과 상관없으므로 머리만 작아진다.
  가리비의 수와 빛의 폭도 줄어든 머리를 따른다
- **종의 살.** 한 톤(0.32)으로 칠하던 것을, 꼭대기 0.18에서 밑단 0.46으로 짙어지는 램프로 칠한다
- **종의 속.** 꼭대기 아래에 옅은 버섯 모양(가리비가 얕고 띠 없는 작은 종)과 그 밑의 기둥이 비치고,
  지붕의 경선을 따라 옅은 세로 결이 꼭대기로 모인다. 모두 종의 살을 옅게 파낸 자리이고 종
  모양으로 오린다. 결의 자리는 마리의 위상에 따라 조금씩 돈다(새 난수를 뽑지 않는다). 손잡이
  **INNER** — 속이 비치는 정도, 0이면 비치지 않는다
- **수염과 다리.** 가장자리의 가는 촉수를 수염, 가운데의 굵은 구완을 다리로 나눠 손잡이를 따로 둔다.
  수염은 TENTACLES·TRAIL·WOBBLE·SWAY, 다리는 ARMS와 새 손잡이 **REACH**(길이)·**GIRTH**(굵기)·
  **SWING**(흐느적이는 몫)을 받는다. 한쪽의 손잡이는 다른 쪽을 건드리지 않는다
- **다리는 종을 따라 자란다.** 다리의 길이는 종 크기에 REACH를 곱한 것이고(기본 4.3배), 굵기는 기본
  BELL(0.13)의 가까운 종에 견준 크기만큼 굵어진다. BELL을 키우면 다리도 길고 굵어지고, 먼 놈은
  짧고 가늘다. 기본 설정의 가까운 놈은 예전보다 굵고(1.6배) 길다
- **흔들림.** 흔들림을 멈춰 있는 S자 자세와 한 바퀴에 한 번 흐르는 파동의 두 겹으로 나눴다. 루프가
  2초라 파동을 더 느리게 돌릴 수 없으므로, 파동의 몫을 줄여 제 자세를 지킨 채 천천히 흐느적거리게
  한다. 수염은 **SWAY**, 다리는 **SWING**이 몫을 정하고 기본은 둘 다 0.35다. 1이면 예전과 같은
  흔들림이다. 다리가 옆으로 흔들리는 폭은 WOBBLE에서 떼어 기본값(0.45)에 묶었다
- **손잡이 묶음.** 판의 손잡이에 group을 달면 조절칸이 묶음마다 작은 제목을 단다. JELLY는
  SWARM·BELL·WHISKERS·LEGS·WATER로 나뉜다
- **구완 수.** ARMS 기본값을 3에서 5로 올렸다. 먼 놈도 하나만 덜어 낸다(예전에는 40%까지) — ARMS
  5면 가까운 놈은 다섯, 먼 놈은 넷이다
- **다리의 난수.** 촉수와 구완의 값을 무리의 흐름에서 뽑던 것을 따로 굴린 흐름으로 옮겼다. 손잡이의
  끝값(촉수 스물, 구완 여섯)만큼 늘 뽑고 앞에서부터 쓴다. 전에는 촉수나 구완의 수를 바꾸면 뒤에
  놓이는 해파리의 자리가 밀렸다. 새 값(촉수가 벌어지는 정도, 구완이 꼬이는 박자와 위상)도 따로
  굴린 흐름에서 마리마다 같은 수만큼 뽑는다

### 확인한 것

- 다리를 그리는 부분만 빼고 찍으면, ARMS를 2에서 5·0·6으로, TENTACLES를 0·20으로 바꿔도 해파리
  아홉의 장이 픽셀까지 같다. 다리 수가 무리의 자리를 건드리지 않는다
- 다리의 난수를 떼기 전에는, 다리를 그리는 부분만 뺀 판이 0.34.0과 세 가지 설정(배색·통 수·무리
  수·FIELD·촉수와 구완의 수를 달리한 것)에서 픽셀까지 같았다. 그 뒤로 물은 손대지 않았고, 종과
  빛은 머리를 바꾸며 위의 항목만큼 달라졌다
- t=1로 찍은 장이 t=0과 씨앗 여섯에서, 기본값과 해파리 아홉(구완 여섯, 촉수 스물) 모두 픽셀까지
  같다. t=1−1e−9는 씨앗 셋 중 하나에서 1바이트 다른데, 0.34.0에서도 같은 씨앗이 1바이트 달랐다
- 수염만 그린 판은 ARMS·REACH·GIRTH·SWING을 끝까지 바꿔도, 다리만 그린 판은 TENTACLES·TRAIL·
  WOBBLE·SWAY를 끝까지 바꿔도 픽셀까지 같다
- 다리를 종에 묶은 뒤에도 t=1로 찍은 장이 t=0과 같다. 기본값과, 해파리 아홉에 다리·수염 손잡이를
  크게 올린 설정에서 씨앗 여섯 모두 픽셀까지 같다
- SWAY 1은 SWAY를 넣기 전과 세 프레임(0·7·29)에서 픽셀까지 같다(수염과 다리를 나누기 전, 둘이 SWAY
  하나를 나눠 쓰던 때에 잰 것이다). 첫 장(t=0)은 SWAY 0·0.35·1에서
  픽셀까지 같다. 박동·떠다님·티끌을 끄고 3프레임 간격으로 잰 이웃 프레임의 평균 차이는 SWAY 1에서
  14.1, 0.35에서 8.7, 0에서 2.1이다(0에서 남는 것은 촉수 굵기가 비틀리며 흐르는 몫이다)
- SWAY 0.6을 더한 설정까지, t=1로 찍은 장이 t=0과 씨앗 여섯에서 픽셀까지 같다
- 머리를 바꾼 뒤에도 t=1로 찍은 장이 t=0과 같다. 위의 두 설정에 PULSE 0.35·THROB 3·INNER 1을
  더한 셋째 설정까지 씨앗 여섯에서 픽셀까지 같다
- INNER만 0에서 1로 바꾼 두 장은 21,761바이트가 다르다. 속은 종 모양으로 오려 가운데 통에서만
  파내므로 달라질 수 있는 자리는 종 안뿐이다
- 손잡이를 끝까지 당겨도, GAP을 0에 두어도, INNER를 0이나 1에 두어도, 다리를 모두 빼도 통
  하나·둘·셋 어디서도 깨지지 않는다
- CELL 3에서 잰 CPU 쪽 시간(GPU를 기다리지 않은 값, 세 번 잰 가운데 값)이 기본 손잡이에서
  2.26 ms가 3.06 ms로, 해파리 아홉에서 4.26 ms가 5.40 ms로 는다. 촉수를 더 촘촘히 짚고 구완이
  길어지고 늘어난 몫과 종의 속을 파내는 몫이다. 머리가 작아진 만큼 빛을 흐리는 몫은 준다

## [0.34.0] - 2026-09-17

MOON과 JELLY의 종을 라인리스로 바꿨다. 테두리를 긋지 않고 면의 경계만으로 형태를 세운다. MOON의
달과 수평선, JELLY의 종 지붕은 자와 컴퍼스로 자른 모양 대신 손으로 오린 모양이다. JELLY 물의
얼룩은 가장자리를 흐려 번진 것처럼 보이고, 종 둘레의 빛은 계단 없이 번진다. 망점은 가장 옅은
잉크를 0부터 키워, 옅어지다 사라지는 자리에 칼로 그은 듯한 윤곽이 서지 않는다. 같은 롤이 0.33.x와
다른 MOON과 JELLY를 찍고, 다른 판도 가장 옅은 자리가 조금 옅어진다(망점 칸이 작을수록 많이).
MOON의 별과 물빛, JELLY의 무리와 촉수와 티끌은 자리가 그대로다.

### 바꿈

- **MOON의 달.** 가장자리가 조금 흔들리게 파낸다. 달무리 고리 둘도 손으로 그은 원이다
- **MOON의 수평선.** 하늘과 물 사이에 긋던 곧은 선을 걷었다. 수평선은 하늘과 물이 만나는
  자리일 뿐이고, 물의 윗선이 두 겹의 잡음을 따라 흔들린다. 물은 사각형 대신 그 윗선을 따르는
  모양으로 칠한다
- MOON의 손잡이 **HAND** — 흔들림의 정도. 0이면 반듯한 원과 곧은 가장자리다
- MOON의 손은 난수를 롤에서 따로 굴려 별과 물빛이 쓰는 흐름을 건드리지 않는다
- **JELLY의 종.** 둘레에 긋던 진한 테를 걷었다. 물을 파내는 폭은 테가 차지하던 만큼 줄여, GAP이
  같으면 흰 틈의 폭도 예전과 같다. 촉수와 구완은 그대로다
- **JELLY의 종 지붕.** 보름달처럼 두 겹의 물결로 살짝 흔들린다. 흔들림은 각도에 매긴 배율이라
  종이 뛰어도 제자리에 있다. 손잡이 **HAND** — 0이면 반듯한 반원이다. 흔들림의 난수는 무리와
  따로 굴린다
- **JELLY의 종 밑단.** 모든 종이 같던 밑단(가운데가 종 높이의 26% 들리고 얕은 물결 여섯)을,
  진짜 해파리의 가장자리처럼 둥근 가리비가 줄지어 늘어진 밑단으로 바꾸고 마리마다 따로 뽑는다.
  들린 높이(16~34%), 들린 꼭대기가 쏠린 쪽, 가리비가 늘어진 깊이와 가리비마다의 크기가 다르고,
  가리비의 수는 종이 클수록 많다(5~16). 가리비는 사인의 절댓값을 1보다 작은 지수로 눌러 봉우리가
  둥글고 골이 좁다. 정수 개라 양 끝에서 지붕과 어긋나지 않고, 촉수의 뿌리도 같은 높이에 붙는다.
  난수는 무리·흔들림과 따로 굴린다
- **JELLY의 밑단 띠.** 가리비를 따라 종과 같은 통으로 한 번 더 찍어 밑단을 진하게 한다. 둘레를
  긋는 테가 아니라 면이고, 종 모양으로 오려 지붕 밖으로 삐지지 않는다. 손잡이 **HEM** — 띠의
  진하기, 0이면 띠가 없다
- **JELLY의 빛.** 크기를 달리한 종 세 겹을 옅게 파내던 것을, 종을 성긴 격자에 칠해 흐린 무늬
  하나로 파낸다. 계단이 없어지고, 겹마다 선 가장자리가 종 위로 되풀이되던 흰 메아리도 사라진다.
  빛은 종 아래로도 조금 번진다. 무늬를 담는 캔버스는 한 장을 돌려 쓰되, 둘레 한 칸을 늘 비워
  앞서 찍은 것이 스며들지 않게 한다
- **JELLY의 얼룩.** 오려 낸 듯 또렷하던 가장자리를 흐린다. 상자 흐림을 세 번 겹치고, 판의
  가장자리도 안쪽과 같게 흐리도록 밭을 넓게 그려 흐린 뒤 잘라 낸다. 손잡이 **FEATHER** — 0이면
  예전처럼 또렷하다
- JELLY의 **GAP** 기본값을 2.5에서 0.5로 줄였다. 종과 촉수 둘레의 흰 틈이 가늘어진다. 주소에
  GAP을 담은 링크는 담긴 값을 그대로 쓴다
- **라인리스.** 테두리 없이 면의 경계만으로 형태를 그리는 이 방식의 이름. MAP.md의 말 목록에 더했다
- **망점의 가장 옅은 자리.** 가장자리 흐림 때문에 반지름 0인 점도 칸 가운데에 반쯤 칠해진 원뿔로
  남아, 잉크가 조금이라도 있으면 칸이 한 번에 덮였다(CELL 3에서 7%쯤, CELL 9에서 1% 아래). 그래서
  옅어지다 0이 되는 얼룩의 윤곽이 칼로 그은 듯 섰다 — JELLY의 물빛 얼룩이 그랬다. 이제 그 원뿔이
  칸에서 덮는 몫(`least`)보다 옅은 잉크는 그 비율만큼 옅게 찍는다. 그보다 진한 자리는 예전과
  같다. how.html의 가장자리 설명과 코드 조각도 고쳤다

### 확인한 것

- MOON은 t=1과 t=1−1e−9로 찍은 장이 t=0과 씨앗마다 픽셀까지 같다. 이음매 z는 여섯 씨앗에서
  +0.93에서 +1.51로, 0.33.0 벤치의 MOON(+0.96에서 +1.34)과 같은 쪽에 비슷한 크기로 모인다.
  달이 오르내리는 빠르기가 이음매 즈음에서 가장 커서 그렇고, 끊긴 것이 아니다
- MOON의 손잡이를 모두 끝까지 올리거나 내려도, HAND를 0에 두어도, 통 하나·둘·셋 어디서도
  깨지지 않는다
- JELLY에서 빛과 밑단 말고 달라진 것은 새 손잡이뿐이다. 밑단을 예전 식으로 되돌리고 HEM을 0에
  두면, 밑단을 손대기 전의 소스를 글자 그대로 다시 지은 판과 세 가지 설정에서 픽셀까지 같다. 그
  판에서 빛을 예전 세 겹으로 바꿔 끼우고 FEATHER와 HAND를 0에 두면, GAP이 같을 때 테만 걷은 종과
  네 가지 설정(배색·통 수·무리 수·FIELD·GAP을 달리한 것)에서 픽셀까지 같다
- HEM만 0에서 1로 바꾼 두 장은 16,718바이트가 다르다. 띠는 종 모양으로 오려 가운데 통에만 찍으므로 달라질 수 있는 자리는 밑단 띠 안뿐이다
- JELLY는 t=1로 찍은 장이 t=0과 씨앗 여섯에서, 기본값과 해파리 아홉 모두 픽셀까지 같다.
  t=1−1e−9는 씨앗 셋 중 하나에서 1바이트 다른데, 바꾸기 전 JELLY도 같은 씨앗에서 똑같이
  다르다(README의 부동소수점 반올림)
- 빛 캔버스를 돌려 써도 앞서 찍은 것에 매이지 않는다. 작은 종, 큰 종, 다시 작은 종을 찍은 장과
  새로 불러온 판이 찍은 장이 픽셀까지 같다
- 손잡이를 끝까지 당겨도, GAP을 0에 두어도, 통 하나·둘·셋 어디서도 깨지지 않는다
- 망점을 고친 앞뒤로 바뀐 픽셀의 비율. 기본 손잡이, 배색 둘(GOLD × SKY × MIST와 RED × AMBER ×
  AQUA)로 한 장씩 찍었다. 식으로 보아 잉크는 줄기만 하므로 바뀐 자리는 전부 옅어진 쪽이다

  | 판 | CELL 16 | CELL 9 | CELL 3 |
  | --- | --- | --- | --- |
  | RIPPLE | 0.00% | 0.01% | 0.29% |
  | MOON | 0.00% | 0.01% | 10.47% |
  | GARDEN | 0.00% | 0.01% | 0.30% |
  | JELLY | 0.02% | 1.04% | 26.90% |
  | CELL | 0.00% | 0.00% | 1.58% |
  | CHLORO | 0.00% | 0.08% | 7.66% |
  | COSMOS | 0.01% | 0.63% | 12.19% |
  | FLAKE | 0.01% | 0.05% | 2.55% |
  | KALEIDO | 0.01% | 0.18% | 11.96% |

- JELLY의 얼룩 경계는 CELL 3에서 가운데 통이 0인 자리를 그대로 따라 섰다(RED × AMBER × AQUA,
  씨앗 11). 고친 뒤에는 그 자리에 경계가 없다. 가운데 통에 2/255의 바닥을 깔아도 경계가 사라져
  원인이 0 언저리의 뜀이라는 것을 따로 확인했다
- JELLY 한 장에 드는 시간이 는다. CELL 3에서 잰 CPU 쪽 시간(GPU를 기다리지 않은 값, 세 번 잰
  가운데 값)으로, 기본 손잡이에서 1.40 ms가 2.17 ms로, 해파리 아홉에서 2.34 ms가 4.01 ms로 된다.
  해파리 아홉에서 밑단 띠의 몫은 0.3 ms쯤이다(HEM 0이면 3.70 ms)

## [0.33.0] - 2026-09-17

CELL을 받은 사진처럼 공초점 현미경으로 본 바이러스 입자로 다시 그렸다. 같은 롤이 0.32.x와 전혀 다른
CELL을 찍고, 손잡이도 바뀌었다. 흰 시야에 물들인 세포가 일렁이고 나뉘던 예전 CELL은 git 기록에 있다.

### 바꿈

- **CELL은 이제 어두운 시야에 빛나는 바이러스 입자다.** 시야는 COSMOS·FLAKE처럼 노랗지 않은 통을
  밤으로 깔고, 빛나는 것은 전부 파낸다. 가장 파란 통은 덜 파내 빛이 흰빛보다 하늘빛을 띠고, 가장
  밝은 끝만 거의 끝까지 파낸다
- 가시는 줄기 끝에 둥글거나 납작한 머리가 달린 곤봉이다. 줄기는 옅게, 머리는 거의 끝까지 파내 끝이
  가장 밝다. 길이가 제각각이고, 넷에 하나는 이쪽을 향해 짧아 보이며 앞면에서 솟는다
- 앞면은 밤을 절반쯤 파내고 테는 더 밝게, 둘레에는 빛이 옅게 번진다. 한가운데는 밤을 도로 얹어 짙은
  속이 되고, 그 둘레에 앞을 보는 가시 머리가 옅은 원으로 박힌다(CUPS). 원 속은 다시 어둡다
- 초점 밖의 입자는 먼저, 크게 번진 옅은 원으로 찍고 속을 조금 어둡게 둔다. 시야는 한가운데가 조금
  밝고, 빛 번짐 몇과 작은 빛 알갱이가 뜬다(MOTES)
- 움직임은 입자마다 따로다. 작은 닫힌 길을 돌고 제자리에서 기울며, 가시는 저마다 다른 박자로
  늘었다 줄고 까딱인다
- 손잡이 열둘 — COUNT · FIELD · SIZE · SPIKES · LENGTH · CUPS · DEPTH · DRIFT · SPIN · GLOW · DARK ·
  MOTES. 시야의 손잡이는 SCOPE 칸의 것이다. 주소에 남은 예전 CELL의 손잡이는 읽을 때 버린다
- `scope.light`가 바닥을 깔 통을 받는다. 넘기지 않으면 예전처럼 가장 옅은 통이다
- 벤치가 두 인쇄기 모두에 판의 SCOPE 기본값을 넘긴다. 옛 인쇄기는 `scope`를 몰라 둥근 틀 판의
  시야 반지름이 비어 있었다

### 확인한 것

- 배색 아홉 벌 모두에서 빛나는 입자로 읽힌다. AQUA × BLUE × LEMON처럼 짙은 파랑이 든 배색이 사진에
  가장 가깝고, 산호·빨강·분홍이 든 배색은 짙은 보라나 남색 시야가 된다
- t=1과 t=1−1e−9로 찍은 장이 t=0과 씨앗마다 픽셀까지 같다. DRIFT와 SPIN과 MOTES를 0에 두면 어느
  프레임이나 같은 장이다. 이음매 z는 여섯 씨앗에서 −1.47에서 +1.41 사이로 부호가 섞여 흩어진다
- 손잡이를 모두 끝까지 올리거나 내려도, 통 하나·둘·셋 어디서도 깨지지 않는다
- CHLORO는 `light`를 고친 뒤에도 네 가지 설정에서 픽셀까지 같다
- 아홉 판을 벤치로 다시 쟀다. 1080 한 장을 GPU를 기다려 잰 평균이 새 인쇄기로 1.2~5.4ms다. CELL
  2.45ms, COSMOS 2.96ms, FLAKE 3.68ms, KALEIDO 1.73ms(M2 Max). 결과는
  `bench/results/2026-09-17-canvas2d-vs-webgl2-nine-plates/`에 있다

## [0.32.0] - 2026-09-17

현미경 아래의 눈 결정 한 송이(FLAKE)와 만화경(KALEIDO)을 걸고, RIPPLE에 물결이 시작되는 자리를
늘리는 손잡이를 더했다. 기본값에서는 기존 판이 모두 그대로다.

### 추가

- **FLAKE.** 벤틀리의 눈 결정 사진처럼 어두운 시야 한가운데 큰 결정 하나. 30° 조각 하나를 여섯 번
  돌리고 한 번씩 뒤집어 열두 벌을 찍어 육각 대칭이 어긋나지 않는다. 뒤집은 조각은 점 순서를 되돌려
  한 경로로 채워도 겹친 자리가 비지 않는다
- 가운데 육각판, 여섯 가지, 곁가지와 잔가지. HABIT이 넓은 판에서 가는 고사리로 옮겨 간다. 몸은
  밤을 옅게 파내고 모서리는 끝까지 파내 희다. 등뼈와 동심 육각은 가장 진한 통으로 새기고(RIDGE),
  공기방울은 대칭을 따르지 않는다(BUBBLE). 가지 끝과 모서리가 저마다 다른 박자로 반짝이고(GLINT),
  작은 결정이 흐리게 떠서 까딱인다(FLURRY)
- 손잡이 아홉 — FIELD · SIZE · HABIT · BRANCH · RIDGE · BUBBLE · GLINT · FLURRY · DARK. 시야의 손잡이는
  SCOPE 칸의 것이다
- **KALEIDO.** 만화경. 쐐기 하나를 MIRRORS번 돌리고 한 번씩 뒤집어 붙인다. 쐐기 안에는 형태 어휘
  그대로의 색유리 조각이 들고, 셋에 하나는 다음 통에 비껴 한 번 더 찍혀 제3의 색이 난다. 조각이
  쐐기 경계를 넘나들어 거울 선에서 꽃처럼 핀다. 조각마다 작은 고리를 돌고 까딱여 무늬가 피었다
  진다. 통 전체는 돌지 않는다
- 손잡이 여섯 — FIELD · MIRRORS · PIECES · SIZE · TUMBLE · TINT. 시야의 손잡이는 SCOPE 칸의 것이다
- **RIPPLE의 DROPS와 FIELD.** 물결이 시작되는 자리의 수. 첫 자리는 언제나 가운데이고 롤의 난수를
  그대로 쓴다. 둘째부터는 따로 굴린 난수로, 이미 놓인 자리에서 가장 먼 곳에 조금 작게 놓이고 제
  박자로 퍼진다. FIELD는 그 자리의 씨앗이다. DROPS 1, FIELD 0이면 예전과 픽셀까지 같다
- **`src/drums.js`.** CHLORO와 COSMOS에 흩어져 있던 "색으로 통 고르기"를 모았다. 가장 초록인 두
  통, 가장 파란 통과 노란 통, 밤과 빛. FLAKE도 쓴다
- 벤치가 FLAKE와 KALEIDO도 견준다

### 확인한 것

- 기존 일곱 판이 네 가지 설정에서 픽셀까지 같다. CHLORO와 COSMOS는 통 고르기를 옮긴 뒤에도 같다
- FLAKE와 KALEIDO, 그리고 RIPPLE의 일곱 자리(FIELD 5)에서 t=1과 t=1−1e−9로 찍은 장이 t=0과
  픽셀까지 같다. 손잡이를 모두 끝까지 올리거나 내려도 통 하나·둘·셋 어디서도 깨지지 않는다
- 이음매 z는 여섯 씨앗에서 FLAKE가 −1.28에서 +2.61, KALEIDO가 −1.66에서 +1.53으로 부호가 섞여
  흩어진다. 일곱 자리 RIPPLE은 −1.85에서 −0.11로, 한 자리일 때처럼 음수 쪽이다
- KALEIDO의 거울 선에 이음 자국이 보이지 않는다

## [0.31.0] - 2026-09-17

망원경으로 들여다본 우주, COSMOS를 걸었다. 둥근 틀을 두르는 판의 시야 손잡이를 한 칸으로 모아
판끼리 나눠 쓰게 하고, 눈금(RETICLE)은 걷었다. 같은 롤이 0.30.x와 다른 CELL을 찍는다 — 눈금이
없다. 주소의 시야 손잡이는 이제 `s`에 실린다.

### 추가

- **COSMOS.** CELL, CHLORO와 같은 둥근 틀에 어두운 시야를 깔고 빛나는 것을 전부 파낸다. 비스듬히
  가로지르는 은하수, 번지는 성운, 기운 나선 은하, 크기가 거듭제곱으로 퍼진 별, 가장 밝은 별의 회절
  십자가 든다
- 통을 밝기가 아니라 색으로 나눈다. 노랗지 않은 통이 밤이 되어 하늘을 깔고, 노란 통은 빛이 되어
  빛나는 자리에만 얹는다. 노랑을 하늘에 깔면 파랑과 곱해져 밤이 초록이 된다. 은하는 한가운데가
  노랗고 팔이 푸르다
- **은하수(MILKY).** 밤을 흐릿하게 파낸 띠에 중심 쪽 따뜻한 빛, 띠를 따라 흐르는 먼지 골짜기, 별
  구름으로 뭉친 아주 작은 별 천사백 개. 은하수의 자리와 별은 다른 것을 다 뽑은 뒤에 뽑아, 은하수를
  더해도 성운과 은하와 별의 자리가 그대로다
- 별만 움직인다. 저마다 박자가 다르게 반짝이고, 대기에 흔들려 제자리에서 떤다. 은하수와 성운과
  은하는 가만히 있다
- 은하수와 성운과 은하는 작은 캔버스에 한 번 구워 늘려 찍는다. 롤과 모양이 같으면 다시 굽지 않고,
  굽는 난수는 자리를 뽑는 난수와 따로 굴린다
- 손잡이 열 — FIELD · STARS · SPIKES · TWINKLE · MILKY · NEBULA · GALAXY · TILT · ARMS · DARK
- **SCOPE 칸.** 둥근 틀을 두르는 판(CELL · CHLORO · COSMOS)은 제 손잡이와 따로 `scope`에 시야
  손잡이 FRAME과 VIGNETTE를 내놓는다. `src/scope.js`에 한 번 적어 두고, 화면은 PLATE KNOBS 아래
  따로 한 칸으로 짓는다. 값은 판마다 기억하지 않고 나눠 쓴다 — 같은 접안렌즈이므로. 틀이 없는 판에서는
  칸이 숨는다
- 인쇄기가 판의 `scope` 기본값도 채운다. 판 하나만 들고 찍어도 그대로 돈다
- COSMOS도 VIGNETTE를 쓴다. 다 그린 뒤 가장 진한 통으로 가장자리를 한 켜 눌러 별과 가스까지 함께
  죽인다
- 벤치가 COSMOS도 견준다

### 바꿈

- 시야 손잡이의 기본값이 하나로 모였다. FRAME 0.44, VIGNETTE 0.5. CHLORO는 0.46과 0.2였고, CELL은
  그대로다. FRAME은 세 판 모두 0.3에서 0.75까지다
- 주소에서 시야 손잡이는 `s`에 따로 실린다. 예전 주소처럼 `k`에 섞여 온 FRAME과 VIGNETTE도 받는다

### 없앰

- **RETICLE.** 시야를 가로지르는 눈금을 CELL, CHLORO, COSMOS에서 모두 걷었다. `src/scope.js`의
  `reticle`도 없앴다. 주소에 남은 옛 값은 읽을 때 버린다

### 확인한 것

- CELL은 0.30.0의 CELL에서 RETICLE을 0으로 둔 것과 세 가지 설정에서 픽셀까지 같다
- 한 판에서 FRAME을 바꾸면 다른 둥근 틀 판도 같은 값을 쓰고, RIPPLE로 가면 SCOPE 칸이 숨는다. 옛
  주소의 `k`에 든 FRAME과 VIGNETTE는 SCOPE 칸으로, RETICLE은 버려진다
- 배색 아홉 벌 모두에서 우주로 읽힌다. 산호·빨강·분홍이 든 배색은 짙은 보라나 남색 밤이, 하늘색
  배색은 밝은 파랑 밤이 된다
- t=1과 t=1−1e−9로 찍은 장이 t=0과 씨앗마다 픽셀까지 같다. TWINKLE을 0에 두면 어느 프레임이나
  같은 장이다
- 초당 여덟 장의 이음매 z는 여섯 씨앗에서 −2.27에서 +1.44 사이로 부호가 섞여 흩어진다. 걸음이
  워낙 고르게 모여 있어 작은 차이에도 z가 커진다
- 손잡이를 모두 끝까지 올리거나 내려도, 은하수와 성운과 은하를 끄거나 시야를 판 가득 넓혀도, 통
  하나·둘·셋 어디서도 깨지지 않는다. 틀 밖 귀퉁이는 언제나 종이다. 같은 프레임을 다른 롤 사이에 두고
  두 번 찍어 픽셀까지 같다

## [0.30.0] - 2026-09-17

CHLORO의 엽록체가 다 같이 밀리지 않고 저마다 조금씩 움직인다. 같은 롤이 0.29.x와 다른 CHLORO를
찍는다. 세포는 그대로이고 엽록체의 자리가 새로 뽑힌다.

### 바꿈

- **엽록체마다 따로 움직인다.** 저마다 제자리에서 몇 픽셀짜리 작은 고리를 돌고 조금씩 기운다.
  고리는 기울기와 모양이 제각각이고, 두 축이 한 바퀴에 한 번이나 두 번 돌아 루프 끝에서 제자리로
  돌아온다. 벽에 붙은 것은 덜 움직이고, 벽은 움직이지 않는다
- 세포 안의 엽록체를 한 물결로 밀던 원형질 흐름과 슬라이드 전체가 도는 길을 걷었다. 한 덩어리로
  흔들려 살아 있는 것보다 화면이 흔들리는 것으로 읽혔다
- 손잡이 STREAM과 DRIFT를 걷고 **WANDER**를 뒀다. 엽록체가 움직이는 폭이고 픽셀로 센다. 기본 3이다.
  주소에 남은 옛 STREAM과 DRIFT는 읽을 때 버린다

### 확인한 것

- 엽록체를 빼고 찍으면 어느 프레임이나 픽셀까지 같은 장이다. 벽과 바탕은 움직이지 않는다
- t=1로 찍은 장이 t=0과 씨앗 셋에서 픽셀까지 같다
- 초당 여덟 장에서 이웃 프레임 걸음이 7.0~7.9로 고르다. 0.29.0의 23~30보다 훨씬 잔잔하다. 여섯
  씨앗의 이음매 z는 −1.48에서 +0.45 사이다
- WANDER를 0이나 10에 두어도, 손잡이를 모두 끝까지 올리거나 내려도, 통 하나·둘·셋 어디서도 깨지지
  않는다

## [0.29.0] - 2026-09-17

CHLORO를 잎 조직으로 다시 그렸다. 받은 현미경 사진처럼, 길쭉한 육각형 세포가 벽을 맞대고
세포마다 엽록체가 가득 찬다. 같은 롤이 0.28.x와 전혀 다른 CHLORO를 찍고, 손잡이도 바뀌었다.

### 바꿈

- **CHLORO는 이제 잎 조직이다.** 엽록체 한 알의 단면(두 겹 막, 라멜라, 그라나 더미)을 크게 그리던
  것을 걷고, 광학 현미경으로 본 검정말이나 이끼의 잎을 그린다
- **세포.** 어긋난 육각 격자의 점을 흔들어 보로노이로 나누고, 한 방향으로 늘여 돌린다. 나눈 뒤에
  늘이므로 이웃 세포의 벽이 언제나 맞물린다. 격자 자리마다 제 씨앗이 있어 시야를 넓히거나 좁혀도
  남는 세포는 그대로다
- **벽.** 긋지 않고 남긴다. 세포마다 벽 두께의 절반만큼 들인 속을 옅은 하늘빛으로 칠하고 테두리를
  진하게 긋는다. 이웃 두 테두리가 한 벽의 양쪽 가장자리가 되고, 그 사이의 띠는 옅은 노랑이다
- **엽록체.** 테가 진하고 속에 알갱이가 몇 박힌 원반이다. 세 켜로 나눠 같은 켜끼리는 거의 겹치지
  않고, 켜가 다르면 깊이 겹쳐 겹친 자리가 더 진하다. 초점이 맞지 않는 것은 먼저, 크고 옅게, 테
  없이 칠한다. 초록은 여전히 곱했을 때 가장 초록인 두 통이 낸다. 세포 속은 가장 파란 통, 벽의 띠는
  가장 노란 통이 칠한다
- **움직임.** 원형질 유동. 세포 한가운데를 한 바퀴에 정수 번 도는 물결이 엽록체를 밀었다 놓고,
  벽에 붙은 것은 덜 밀린다. 엽록체는 제자리에서 떨고 기울며, 슬라이드 전체가 고정된 시야 밑에서
  작은 타원을 돈다. 두 축의 위상을 맞춰 한 줄 위를 오가다 멈추는 일이 없다
- 조직은 시간과 무관해서 한 번 짜 두고 프레임마다 다시 쓴다. 손잡이를 오가도 몇 벌은 쥐고 있다
- 손잡이 열여섯 — FIELD · SIZE · STRETCH · ANGLE · JITTER · WALL · DENSITY · PLASTID · DEPTH · STREAM
  · DRIFT · TINT · GROUND · VIGNETTE · RETICLE · FRAME. FRAME을 0.72 넘게 올리면 시야가 판을 다
  덮어 사진처럼 가득 찬다. 부스러기는 두지 않는다
- README의 CHLORO 대목과 시간 표를 새 판에 맞췄다

### 확인한 것

- t=1로 찍은 장이 t=0과 씨앗 셋에서 픽셀까지 같다. t=1−1e−9는 수백 바이트가 다르지만 t=1e−9도
  t=0과 그만큼 다르다. 작은 타원 수천 개를 긋는 캔버스가 아주 작은 이동에 반응하는 것이지 이음매가
  아니다
- 초당 여덟 장에서 이웃 프레임 걸음이 한 바퀴 내내 고르고(23~30), 여섯 씨앗의 z가 −1.59에서
  +1.32 사이에 흩어진다. 벤치에서도 두 인쇄기가 씨앗마다 같은 z를 낸다
- 손잡이를 모두 끝까지 올리거나 내려도, 시야를 판 가득 넓혀도, 통 하나·둘·셋 어디서도 깨지지
  않는다. 같은 프레임을 다른 롤 사이에 두고 두 번 찍어 픽셀까지 같다. 배색 아홉 벌 모두에서 엽록체가
  초록이다
- 1080 한 장을 GPU를 기다려 잰 평균은 6.12ms, 99분위 7.3ms다(M2 Max). 그중 판 짜기가 3.59ms다.
  캔버스 2D 인쇄기로는 98.67ms다. 결과는
  `bench/results/2026-09-17-canvas2d-vs-webgl2-leaf-tissue/`에 있다

## [0.28.0] - 2026-09-17

SLIDE의 이름을 CELL로 바꾸고, 엽록체를 들여다보는 CHLORO를 걸었다. 주소의 `plate=slide`는 이제 첫
판을 연다. RIPPLE이 루프 끝에서 튀던 것을 고쳐, 같은 롤이 0.27.x와 조금 다른 RIPPLE을 찍는다.

### 바꿈

- **SLIDE → CELL.** 이름만 바뀌었다. 세포는 0.26.0의 모습 그대로다
- **README를 영어로 새로 썼다.** 판 여섯, 시야 모듈, t를 1 바로 앞에 두고 찍는 확인, 여섯 판으로
  다시 잰 벤치 값까지 지금에 맞췄다. MAP.md와 how.html과 bench/README.md는 한국어 그대로다

### 추가

- **CHLORO.** 현미경으로 들여다본 엽록체. 렌즈꼴 몸에 막이 두 겹이고, 스트로마 라멜라가 긴 축을
  따라 지나며 끝에서 모인다. 그 위에 틸라코이드 원반이 동전처럼 쌓인 그라나가 라멜라를 하나 건너
  앉는다. 스트로마에는 리보솜 알갱이와 진한 기름방울이 흩어지고, 녹말 알갱이는 모든 통에서 파낸 흰
  타원에 옅은 테를 두른다. 나뉘는 것은 허리가 조여 아령꼴이 되고 한 바퀴에 한 번 조였다 풀린다
- CELL을 더 세포답게 다시 그려 보다 나온 모습 — 노란 몸에 방추처럼 모이는 선과 짧은 막대 — 이
  엽록체로 읽혀, 그 모습을 엽록체의 구조로 옮겼다
- 초록 잉크가 없으므로 판이 배색을 보고, 곱했을 때 가장 초록인 두 통으로 스트로마를 칠한다. 아홉
  배색 가운데 두 통이 모두 노랑인 MUSTARD × LEMON의 2도만 가장 초록인 한 통으로 칠한다
- 손잡이 열여섯 — COUNT · FIELD · SIZE · LENS · GRANA · STACK · LAMELLAE · DIVIDE · DRIFT · WOBBLE ·
  TINT · STROMA · DEBRIS · VIGNETTE · RETICLE · FRAME. 눈금과 비네트는 옅게, 부스러기는 적게 두는 것이
  기본이다
- **`src/scope.js`.** 현미경의 시야 — 가장자리로 갈수록 죽는 빛, 부스러기, 눈금 — 를 CELL에서 꺼내
  두 판이 함께 쓴다
- 벤치가 CELL과 CHLORO도 견준다. 여섯 판으로 다시 잰 결과를
  `bench/results/2026-09-17-canvas2d-vs-webgl2-six-plates/`에 남겼다

### 고침

- **RIPPLE의 이음매.** 호의 두 번째 흔들림이 한 바퀴에 1.5번 돌아, 루프 끝에서 고리가 1픽셀 남짓
  튀었다. 2번 돌게 했다. t를 1 바로 앞에 두고 찍은 장이 t=0과 3만 4천 바이트쯤 달랐는데 이제 같다
- 벤치 보고서의 그림 이름이 판 수와 상관없이 "네 판 전체"였다

### 확인한 것

- CELL은 SLIDE와 네 가지 설정에서 픽셀까지 같다. 기본, 세포 스물에 좁은 시야, 2도에 모두 나뉨,
  눈금과 비네트와 부스러기를 끄고 알갱이 열둘
- t를 1 바로 앞에 두고 찍은 장이 t=0과 같다. RIPPLE · MOON · GARDEN · CELL · CHLORO는 픽셀까지
  같고, JELLY는 부동소수 반올림으로 1바이트가 다르다. CHLORO는 손잡이를 모두 끝까지 당긴 채로도 같다
- CHLORO의 이웃 프레임 걸음은 이음매 앞뒤로 매끄럽게 오르내리고, 여섯 씨앗의 z가 −1.66에서 +1.9
  사이에 흩어진다. 옛 인쇄기도 씨앗마다 같은 값을 낸다
- CHLORO는 손잡이를 모두 끝까지 올리거나 내려도, 통 하나·둘·셋 어디서도 깨지지 않는다
- 1080 한 장을 GPU를 기다려 잰 평균은 CELL 1.89ms, CHLORO 2.47ms다(M2 Max). 캔버스 2D 인쇄기로는
  72ms와 71ms다

## [0.27.0] - 2026-09-17

JELLY에서 둥근 틀을 뺐다. 같은 롤이 0.26.x와 다른 JELLY를 찍는다.

### 없앰

- **JELLY의 FRAME.** 둥근 틀은 SLIDE의 것으로 두고, JELLY는 다시 판 가득 찍는다. 무리가 틀 안쪽에
  앉도록 자리를 깎던 것도 함께 걷었다. 얼룩과 흰 틈과 가늘어지는 촉수는 그대로다
- 틀은 `src/roundel.js`에 남아 있다. 다른 판도 마지막에 한 줄 부르면 두를 수 있다

### 추가

- **`src/mask.js`.** 틀에서 원 바깥을 지우던 몫을 따로 뺐다. 남길 모양 하나의 바깥을 모든 통에서
  파낸다. `maskCircle`은 원을, `maskShape`는 점으로 이은 모양을 곡선이나 꺾인 선으로, `mask`는
  아무 경로나 남긴다. 테 없이 모양대로 잘라 쓸 수 있다
- `src/roundel.js`는 이제 원 마스크에 테(`rim`)를 얹은 것이다. 테만 따로 그을 수도 있다

### 확인한 것

- 새 JELLY는 0.26.0의 JELLY에서 FRAME을 0으로 둔 것과 세 가지 설정에서 픽셀까지 같다
- SLIDE는 마스크를 빼내기 전후로 세 가지 설정에서 픽셀까지 같다
- 원을 한가운데가 아닌 자리에 남겨도, 잎 모양을 남겨도, 꺾인 별 모양을 남겨도 안쪽만 찍히고
  바깥은 종이다. 스스로 겹치는 별은 적어 둔 대로 겹친 가운데가 지워진다
- 주소에 남은 옛 FRAME 값은 읽을 때 버린다

## [0.26.0] - 2026-09-17

현미경으로 들여다본 세포를 걸었다.

### 추가

- **SLIDE.** 둥근 틀이 접안렌즈의 시야가 되고, 그 안에 물들인 세포가 떠 있다. 시야는 한가운데가
  밝고 가장자리로 갈수록 빛이 죽으며, 옅은 눈금 십자가 지나간다
- 세포질은 가운데 통과 옅은 통에 나눠 찍어 겹친 자리에서 제3의 색이 난다. 막과 핵과 작은 기관은
  가장 진한 통이고, 소포는 세포질에서 파낸 구멍, 핵 안의 인은 진한 통에서만 파낸 자리다
- 세포는 작은 닫힌 길을 돌고, 막은 제자리에서 일렁이고, 기관은 세포 안을 한 바퀴 흘러 돈다.
  나뉘는 세포는 두 몸이 벌어졌다 붙었다 한다
- 손잡이 열둘 — COUNT · FIELD · SIZE · DRIFT · WOBBLE · DIVIDE · TINT · GRANULES · DEBRIS ·
  VIGNETTE · RETICLE · FRAME
- **`src/roundel.js`.** 둥근 틀을 JELLY에서 꺼내 두 판이 함께 쓴다. JELLY는 세 가지 설정에서
  꺼내기 전과 픽셀까지 같다
- 벤치가 SLIDE도 견준다

### 확인한 것

- 1080 한 장이 2.3ms 안팎이다
- t를 1 바로 앞에 두고 찍은 장이 t=0과 픽셀까지 같다. SLIDE와 JELLY 모두 그렇다
- 이음매 z는 씨앗 여섯에서 양수 쪽으로 기울지만, 걸음의 크기가 한 바퀴 안에서 매끄럽게 오르내리고
  끝 걸음만 튀지 않는다. 위의 확인과 함께 보면 끊긴 것이 아니다
- 틀 밖 네 귀퉁이에 잉크가 없고, 같은 프레임을 두 번 찍어 픽셀까지 같다. 손잡이를 끝까지 당겨도
  깨지지 않는다

## [0.25.0] - 2026-09-17

JELLY를 둥근 틀에 넣고, 물에 얼룩을 번지게 하고, 해파리 둘레에 흰 틈을 두었다. 같은 롤이
0.24.x와 다른 JELLY를 찍는다.

### 추가

- **STAIN.** 물에 번진 얼룩. 옅은 통과 가운데 통이 서로 다른 저주파 밭을 따라 번져, 물 위에서
  초록이나 보라 같은 제3의 색이 된다. 롤마다 한 벌이고 시간과 무관하다. 0이면 얼룩이 없다
- **FRAME.** 둥근 틀의 반지름. 틀 밖은 모든 통에서 파내 종이로 두고, 가장 진한 통으로 모양이
  조금 다른 테를 두 번 그어 손으로 찍은 듯 거칠게 한다. 무리는 틀 안쪽에 앉도록 자리를 고르고,
  틀에 걸린 놈은 잘린 채로 남는다. 0이면 예전처럼 판 가득 찍는다
- **GAP.** 해파리 둘레의 흰 틈. 물을 해파리보다 이만큼 넓게 파내, 판이 어긋나지 않아도 가는 종이
  틈이 선다. 촉수는 종의 절반만 벌린다. 0이면 틈 없이 겹친다
- `shapes.ribbon`은 굵기가 변하는 선을 닫힌 띠로 돌려준다. `shapes.grow`는 닫힌 모양을 둘레로
  고르게 키운다. 배율로 키우면 들린 밑단이 오히려 안으로 들어온다

### 바뀜

- **촉수가 뿌리에서 굵고 끝으로 가늘어진다.** 비틀리듯 굵기가 오르내리고, 절반은 속에 흰 줄이
  파여 있다. 물과 같은 통이라 틈이 없으면 묻히므로, 몸을 물보다 진하게 꽉 채워 찍는다
- 구완도 끝으로 가며 조금 좁아진다
- 종을 촉수 뒤에 찍는다. 종이 제 촉수의 뿌리를 덮는다
- 티끌을 실제로 도는 통 모두에서 파낸다. 얼룩이 생긴 뒤로 두 통만 파내면 티끌에 색이 남았다

### 고침

- 통이 둘일 때 빛을 같은 분판에서 두 번 파내, 셋일 때보다 세게 파였다

### 확인한 것

- 기본값으로 1080 한 장이 4.4ms 안팎이다. 얼룩을 고품질로 키워 붙이던 첫 판은 8~11ms였는데,
  보통 품질로 바꾸자 얼룩이 없을 때와 같아졌다. 해파리 아홉 마리면 6.3ms다
- 틀 밖 네 귀퉁이에 잉크가 앉은 픽셀이 없다
- 같은 프레임을 다른 프레임과 다른 얼룩 세기 사이에 두고 찍어도 픽셀까지 같다
- 이음매는 씨앗 여섯에서 +0.76부터 −3.64까지 부호가 섞여 흩어진다. 큰 값은 모두 음수라 끊긴 것이 아니다

## [0.24.0] - 2026-09-17

RIPPLE의 가운데 점을 비껴 찍고, 종이를 희게 했다. 같은 롤이 0.23.x와 다른 장을 찍는다.

### 바뀜

- **RIPPLE의 가운데 점을 두 통이 비껴 찍는다.** 한 통씩만 찍힌 초승달 둘과 그 사이의 겹친
  자리가 함께 보인다. 정확히 포개어 찍던 때는 확대하면 두 망점이 뒤섞인 얼룩이었고, 두
  잉크로 읽히지 않았다. 방향은 롤이 정하되 대개 진한 통이 위, 짝이 아래다
- **짝을 가장 옅은 통에서 가운데 통으로 바꿨다.** 가장 옅은 통은 기본 배색에서 안개색이라
  비껴 찍어도 초승달이 보이지 않았다. 기본 배색에서는 이제 겹친 자리가 초록이 된다
- **가운데 점의 모양을 원에서 손으로 오린 듯한 타원으로.** 두 통의 모양이 조금씩 다르고
  세로로 살짝 길다
- DOT 기본값을 8에서 15로 올렸다. 8픽셀로는 제 크기로 봐도 비껴 찍은 것이 드러나지 않았다
- **종이를 희게.** 따뜻한 크림색에서 거의 흰색으로 바꾸고, 종이결도 중성에 가까운 옅은 회색으로
  맞췄다. 종이가 색을 보태지 않으니 잉크와 겹친 색이 제 색 그대로 앉는다
- **RIPPLE과 GARDEN이 바탕을 깔지 않는다.** 두 판은 가장 옅은 통을 판 전체에 옅게 깔았는데,
  노랑 계열 배색에서는 그 한 겹이 흰 종이를 도로 누렇게 덮었다. 이제 흰 종이가 그대로 바탕이다.
  MOON과 JELLY의 옅은 통은 물 자체라서 그대로 둔다
- HOW IT'S MADE의 종이 단원을 흰 종이에 맞춰 고쳐 썼다

### 추가

- **RIPPLE의 OFFSET.** 가운데 점의 두 통을 얼마나 비껴 찍을지. 점 반지름에 대한 비율이고
  0이면 포갠다

### 확인한 것

- 이음매는 씨앗 여섯에서 z가 −1.42에서 −1.98로, 고치기 전과 사실상 같다. 난수는 점의 모양과
  방향에 한 번씩만 쓰므로 프레임과 무관하게 같은 흐름이다
- 같은 프레임을 다른 프레임 사이에 두고 두 번 찍어 픽셀까지 같았다
- 바탕 층을 뺀 RIPPLE은 가장자리 60픽셀 띠에 잉크가 앉은 픽셀이 하나도 없다. GARDEN의
  이음매도 씨앗 여섯에서 고치기 전과 같다

## [0.23.0] - 2026-09-17

망점이 찍히는 과정을 페이지로 적었다.

### 추가

- **how.html.** 분판에서 종이까지, 잉크 한 통이 찍히는 순서를 열 단계로 따라간다. 분판, 격자,
  반지름, 가장자리, 잡음, 각도, 어긋남, 곱하기, 종이, 그리고 GPU로 옮긴 까닭
- 그림은 모두 이 화면의 인쇄기가 그 자리에서 찍는다. 코드 조각도 셰이더 소스에서 그대로 꺼낸다.
  셰이더를 고치면 페이지도 함께 바뀐다
- 첫머리의 MOON 위로 커서를 옮기면 그 자리를 세 배로 본다. CELL, GRAIN, REGISTER를 돌려 볼 수
  있다
- 화면의 SHEET 카드에 HOW IT'S MADE 연결
- 인쇄기에 `angles`를 넘기면 통마다 스크린 각도를 바꿔 찍는다. 무아레를 보여 주려고 넣었다
- 인쇄기가 찍은 뒤 통마다의 역할, 잉크, 각도, 어긋남을 `passes`로 돌려준다

### 바뀜

- 정적 서버가 `.md`를 글로, `.jpg`를 그림으로 내준다. 페이지에서 벤치 보고서로 바로 넘어간다

## [0.22.0] - 2026-09-17

GARDEN의 틀을 걷었다.

### 없앰

- **GARDEN의 머리띠와 바닥 블록.** 둘 다 이 판에 글자가 있던 때 제목과 문장을 받치던 틀이다.
  글자를 뺀 뒤로는 받칠 것이 없었고, 잎이 물리는 자리에도 닿지 않아 이 판이 보여주려는
  겹침에 보태는 것이 없었다. 걸린 판 가운데 틀을 두른 것도 GARDEN뿐이었다
- 둘 다 난수를 쓰지 않던 것이라 같은 롤이면 잎과 줄기와 열매는 그대로다. 틀만 사라진다

### 바뀜

- **벤치가 두 인쇄기에 같은 판화를 먹인다.** 판화는 새 쪽의 것을 쓴다. 판화를 고친 커밋과
  견주면 판화가 달라진 만큼이 인쇄기의 차이로 잡히기 때문이다

## [0.21.0] - 2026-09-17

망점부터 안착까지를 GPU로 옮겼다. 굽지 않고 찍으면서 튼다.

### 추가

- **bench/.** 옛 인쇄기를 커밋에서 바로 꺼내 새 인쇄기와 한 페이지에서 견준다. 한 장 찍는
  시간, GPU 단계, 톤, 판마다의 블록 차이, 종이결, 이음매, 비교 그림을 한 번에 재서
  `bench/results/`에 적는다. `node bench/serve.mjs`로 띄운다
- 이번 결과는 `bench/results/2026-09-17-canvas2d-vs-webgl2/`에 있다

### 바뀜

- **찍는 일이 셰이더 한 번이 되었다.** 분판은 여전히 2D 캔버스에 긋고, 그것을 텍스처로 올려
  망점·잉크·종이결·판 어긋남·곱하기를 프래그먼트 셰이더 한 번에 찍는다. 1080 한 장이
  59~106밀리초에서 1.4~2.4밀리초가 되었다. 순수 WebGL2다. 빌드 도구는 여전히 없다
- **필름을 굽지 않는다.** 재생도, 손잡이를 끄는 동안도 1080 제 크기로 프레임마다 찍는다.
  PRINTING, LIVE, DRAFT와 손이 멎기를 기다리던 반 초가 사라졌다
- **다이얼이 다음 프레임에 닿는다.** 재생 중에 무엇을 건드려도 멈추지 않고 새 값으로 이어진다
- **다시 틀면 멈춘 프레임에서 이어진다.** 예전에는 0번 프레임부터 다시 돌았다
- **멈춰서 프레임을 끌어도 재생에 나오는 장만 보인다.** 초당 여덟 장이면 0·1·2 프레임이 모두
  0번 장이다. 예전에는 필름이 구워져 있을 때만 그랬다
- **잡음을 픽셀 자리와 롤로 그 자리에서 만든다.** 롤마다 117만 개씩 뽑아 두던 표와 그것을
  스무 벌 쥐던 저장소가 없어졌다. 같은 롤이 0.20.x와 다른 장을 찍는다
- 얼룩이 판형 좌표를 따른다. 콘택트 시트의 작은 장이 얼룩을 세 배로 키워 입지 않는다
- 판 어긋남을 찍은 판을 옮겨 붙이지 않고, 어긋난 자리에서 바로 망점을 친다. 옮기며 한 번 더
  흐려지던 것이 없다
- 콘택트 시트 한 벌이 123밀리초에서 14밀리초가 되었다. 칸마다 인쇄기에 작게 찍어 2D
  캔버스에 옮겨 붙인다
- 분판 종이를 크기마다 세 장 두고 닦아 쓴다. 한 장마다 새로 만들면 초당 스물네 번 GPU 메모리를
  잡았다 놓는다
- 주소를 손이 멎은 뒤에 한 번 적는다. 사파리는 30초에 백 번을 넘으면 주소 고치기를 막는다
- WebGL2를 열지 못하는 브라우저에서는 찍지 못한다고 알린다. GPU가 기계를 잃었다가 되찾으면
  걸려 있던 것을 다시 찍는다

### 확인한 것

옛 인쇄기와 새 인쇄기를 한 페이지에 올려 같은 롤로 견줬다. 잡음의 원천이 바뀌었으니 픽셀이
같을 수는 없다. 같아야 하는 것은 망점 계산과 톤이다.

- GRAIN 0, REGISTER 0에서 통 하나를 톤마다 깔고 종이로 나눠 커버리지를 픽셀마다 견줬다.
  평균 차이 0.0014, 99분위 0.0063 — 8비트로 한두 단계다
- GRAIN 0이면 여섯 판 모두 27픽셀 블록 평균이 최대 1단계 안에서 같다. REGISTER 2에서는 최대
  3단계다
- 기본 GRAIN에서는 통 셋의 톤 곡선이 0.005 안에서 겹치고, 판마다 평균 색이 1단계 안에서 같다.
  블록 차이가 MOON과 JELLY에서 커지는 것은 얼룩의 무늬가 달라서다
- 콘택트 시트 크기에서도 판마다 평균 색이 1단계 안에서 같다
- 종이결만 찍은 장의 평균이 소수 둘째 자리까지 같다

### 재는 법

한 장의 값은 장마다 한 픽셀을 읽어 GPU가 일을 마칠 때까지 기다려 쟀다. 기다리지 않고 재면
판을 짜는 시간만 잡힌다. M2 Max에서 판마다 96장을 찍은 평균이고, 옛 인쇄기는 여섯 장이다.
옛 것은 끓음을 켜면 인상마다 잡음 표를 새로 뽑느라 더 느려지므로 HELD로 쟀다.

| 판 | 캔버스 2D | WebGL2 | WebGL2 99분위 |
| --- | --- | --- | --- |
| RIPPLE | 59 ms | 1.4 ms | 4.0 ms |
| MOON | 95 ms | 1.7 ms | 2.9 ms |
| GARDEN | 71 ms | 1.5 ms | 2.9 ms |
| JELLY | 106 ms | 2.4 ms | 3.3 ms |

WebGL2 한 장을 나누면 판 짜기가 0.03~0.6밀리초, 분판을 래스터하고 올리는 데 0.6~1.1밀리초,
셰이더 한 번이 GPU 시계로 0.2~0.3밀리초다. 시간은 이제 망점이 아니라 분판을 GPU로 옮기는 데
가장 많이 간다.

값은 실행마다 흔들린다. 옛 인쇄기는 10%쯤, 새 인쇄기는 1밀리초 안팎이다. 표는 저장해 둔
실행의 값이다.

이음매는 렌더러를 바꿀 때 같은 씨앗으로 옛 것과 새 것을 나란히 쟀다. 네 판 모두 씨앗마다 z가
거의 같다. RIPPLE은 늘 −1.5에서 −2.0, MOON은 늘 +1.0에서 +1.3으로 한쪽에 모이는데, 두
렌더러가 똑같이 그러므로 판화의 움직임이 루프 안에서 고르게 빠르지 않아서다. 렌더러와는
무관하다.

## [0.20.0] - 2026-09-16

판형 1080, 그리고 화면 크기가 더는 흔들리지 않는다.

### 바뀜

- **판형이 1080×1080이 되었다.** 같은 롤이 0.19.x와 다른 장을 찍는다
- 그리는 크기도 함께 올라갔다 — 끄는 동안 454, 재생 670, 멈춰 서면 1080

### 고침

- **그리는 크기가 바뀔 때마다 화면의 그림까지 작아졌다 커졌다.** 캔버스의 픽셀 수가 곧
  표시 크기였기 때문이다. 달라진 것은 해상도뿐인데 그림이 뛰면 무엇이 바뀌었는지 알 수 없다
- CSS의 `aspect-ratio`로 고치려다 비율이 깨졌다. 높이를 채우게 두면 `max-width`가 가로만
  잘라 정사각 판이 639×783으로 늘어난다. 남은 자리를 재서 직접 정하도록 바꿨다
- 창 크기가 바뀌어도 다시 맞춘다

## [0.19.0] - 2026-09-16

손잡이를 끄는 동안 화면이 따라온다.

### 바뀜

- **끄는 동안에는 작게, 바로 찍는다.** 굽지 않고 프레임마다 그 자리에서 찍는다. 420 크기면
  한 장에 23밀리초라 초당 여덟 장은 물론 스물네 장도 들어오므로, 끄는 손과 화면이 같이
  움직인다. 따라가지 못하면 프레임이 떨어질 뿐 박자는 어긋나지 않는다
- **끄는 동안 제 크기로 찍고 있었다.** 한 장에 107밀리초가 드는 일을 값이 바뀔 때마다 했다.
  그것이 무겁던 이유다
- 멈춰 있을 때 끄는 것도 가벼운 한 장으로 바꿨다. 움직이지는 않지만 손은 따라온다
- 모든 다이얼이 같은 길을 쓴다. 판화를 바꾸든 배색을 바꾸든 새 롤을 굴리든, 바로 가벼운 한
  장이 뜨고 손이 멎은 뒤 제 것이 온다

## [0.18.0] - 2026-09-16

무리에 제 씨앗을 준다.

### 추가

- **FIELD.** 값이 아니라 씨앗인 손잡이다. 무리를 놓는 밭과 크기를 정하는 밭이 이 숫자에서
  나오므로, 끌면 잉크도 종이결도 티끌도 그대로인 채 배치만 다시 뽑힌다. 마음에 드는 인쇄
  상태를 잡아 놓고 구도만 골라 볼 수 있다
- 종이의 롤을 섞어 만들므로 NEW ROLL은 여전히 배치까지 바꾼다. 같은 롤에 같은 FIELD면
  언제나 같은 무리가 나온다
- 손잡이에 설명을 달 수 있다. 마우스를 올리면 뜬다

### 확인한 것

해파리를 0마리로 두고 FIELD를 돌렸을 때 그림이 픽셀까지 같은 것, 무리가 있을 때는 달라지는
것, 같은 FIELD가 같은 무리를 내는 것, 롤을 바꾸면 전부 달라지는 것.

## [0.17.0] - 2026-09-16

무리를 노이즈 밭 둘로 짠다.

### 바뀜

- **자리는 가운데로 모이는 밭에서.** 후보를 열넷 뽑아 점수가 가장 높은 데로 간다. 점수는
  셋을 더한 것이다 — 가운데로 당기는 기운, 밭이 뭉쳐 있는 정도, 이미 놓인 놈에게서 떨어진
  정도. 고르게 흩뿌리지 않으므로 무리가 뭉치고 비는 데가 생긴다
- **크기는 다른 밭에서.** 같은 밭에서 뽑으면 가운데가 늘 크고 가장자리가 늘 작아져 규칙이
  눈에 먼저 읽힌다. 밭을 따로 두면 큰 놈이 구석에도 선다
- 깊이는 크기에서 나온다. 따로 뽑으면 큰데 흐린 놈이 생긴다

### 고침

- **크기가 전부 고만고만했다.** 값 노이즈는 네 귀퉁이를 섞어 만들므로 값이 가운데로 몰린다.
  두 겹으로 겹쳐 잔결을 주고 가운데에서 밀어내 대비를 되찾았다

## [0.16.1] - 2026-09-16

### 고침

- **무리가 사다리로 보였다.** 깊이를 차례대로 한 칸씩 멀어지게 두어, 크기도 높이도 순서대로
  줄어들었다. 마리마다 깊이를 새로 뽑고 크기의 흔들림도 넓혔다
- 자리를 가로로만 벌리고 있었다. 세로까지 흩고, 겹침 판단도 가로 거리에서 두 점 사이 거리로
  바꿨다. 조금 겹치는 것은 그대로 둔다 — 가림이 깊이를 말해 준다

## [0.16.0] - 2026-09-16

해파리 여러 마리.

### 추가

- **COUNT와 DEPTH.** 한 마리부터 아홉까지. 멀수록 작고 흐리고 위에 있다
- 먼 것부터 찍고 가까운 것이 그 위를 덮는다. 가까운 놈의 녹아웃이 먼 놈을 지우므로 가림이
  저절로 생긴다 — 앞뒤를 따로 계산하지 않는다
- 마리마다 박자가 어긋난다. 종이 뛰는 때도, 촉수가 굽이치는 때도 다르다

### 바뀜

- 난수를 무리를 짜는 동안 다 쓰고 그리는 동안에는 쓰지 않는다. 그리기는 먼 것부터 도는데
  거기서 난수를 당기면 마리 수를 바꿀 때마다 무리 전체가 다시 뽑힌다

### 재는 법

이음매를 한 번 재고 판단하지 않기로 했다. 초당 여덟 장이면 걸음이 열여섯뿐이라 표준편차가
흔들리고, 아무 이상이 없어도 z가 2를 넘는 씨앗이 나온다. 씨앗 여섯으로 재서 +2.07에서
−1.90까지 부호를 바꿔 가며 흩어지는 것을 보고 잡음으로 판정했다.

## [0.15.0] - 2026-09-16

해파리 종이 너무 크게 뛰었다.

### 바뀜

- **PULSE 기본값을 0.14에서 0.06으로.** 한 바퀴 동안 종의 너비가 25.5% 벌어지던 것이
  11.3%가 되었다. 청록 화소의 가로 폭을 프레임마다 재서 확인했다
- **THROB를 따로 뺐다.** 한 바퀴에 몇 번 뛰는가. 세기와 빠르기는 다른 것이라 같은 값으로
  묶을 수 없다. 정수여야 한 바퀴 끝에서 제자리로 돌아온다
- **DRIFT를 따로 뺐다.** 해파리 전체가 위아래로 까닥이는 폭

### 고침

- 촉수 안의 지역 변수가 새 손잡이 이름을 가리고 있었다. 동작은 맞았지만 헷갈리는 자리다

## [0.14.0] - 2026-09-16

해파리.

### 추가

- **JELLY.** 어두운 물을 밝은 것 하나가 올라간다. 흰 잉크가 없으므로 빛나는 것은 전부 파낸
  자리다 — 물을 깔고, 종 모양으로 파내고, 그 안에만 옅은 통을 얹는다
- 빛은 한 번에 파내지 않는다. 바깥 테를 옅게 파고 안으로 갈수록 깊이 파, 네 겹으로 번지게
  했다. 한 번에 파내면 오려 붙인 스티커가 된다
- 오르는 것처럼 보이게 하는 것은 해파리가 아니라 물이다. 티끌을 아래로 흘려 보낸다.
  해파리가 실제로 올라가면 한 바퀴 끝에서 제자리로 돌아오느라 튄다
- 손잡이 여덟 — BELL · PULSE · TENTACLES · TRAIL · WOBBLE · ARMS · MOTES · DEEP

### 고침

- 티끌의 속도를 실수로 주고 있었다. `(자리 + t × 속도) % 1`은 속도가 정수일 때만 t=1에서
  제자리로 돌아온다. 한 바퀴에 한 번이나 두 번 건너도록 고쳤다

### 재는 법

이음매를 평균 대비 몇 배인가로만 보던 것을 표준편차로 바꿨다. 걸음이 고르게 모여 있으면
아무 걸음이나 최대가 될 수 있어, 1.3배라는 말이 튄다는 뜻도 아니기 때문이다. 걸린 네 판은
−1.2에서 1.7σ 사이에 있다.

## [0.13.0] - 2026-09-16

GARDEN이 돌아왔다.

### 추가

- **GARDEN.** 밑동을 한 자리에 모아 부챗살로 올린 잎 다발. 잎 한 장이 한 통이라 물린 자리마다
  다른 색이 난다. 통 셋으로 낼 수 있는 색은 셋이 아니라 겹치는 조합만큼이라는 것을 보는 판이다
- 손잡이 여섯 — STEMS · REACH · LEAF · SWAY · SIDE · BERRIES

### 바뀜

- 걸린 판이 되었으므로 GARDEN에서도 글자를 뺐다. 바닥을 받치는 블록만 남겼다
- 잎 크기와 뻗는 길이를 종이 크기의 비율로 돌렸다. 픽셀로 박혀 있어 판형을 바꾸면 무너졌다

## [0.12.0] - 2026-09-16

판마다 제 손잡이를 내놓는다.

### 추가

- **PLATE KNOBS.** 판이 `knobs` 목록을 내놓으면 화면이 그것만 보고 조절칸을 짓는다. 공유
  다이얼이 아니다 — RIPPLE에 HORIZON이 없고 MOON에 RINGS가 없다
- RIPPLE은 RINGS · REACH · EASE · ARCS · WEIGHT · SQUASH · DOT,
  MOON은 MOON · HORIZON · RISE · SKY · STARS · GLINTS
- 값은 판마다 따로 기억한다. 판을 바꿨다 돌아와도 맞춰 둔 것이 남아 있고, 한 판의 값이
  다른 판에 새어 들어가지 않는다. 주소에는 지금 걸린 판의 것만 싣는다
- 화면이 넘겨 주지 않은 손잡이는 판이 스스로 적어 둔 기본값으로 채운다. 판 하나만 들고
  찍어 보는 자리에서도 그대로 돈다

## [0.11.0] - 2026-09-16

RIPPLE을 비웠다.

### 바뀜

- **고리 몇 개와 부서진 호, 가운데 점 하나가 전부다.** 계 넷에 고리 열넷을 놓아 면을
  채우고 있었는데, 면을 채우면 물결은 무늬가 되고 무늬는 퍼지는 것으로 보이지 않는다.
  계 하나에 고리 셋, 나머지는 종이로 둔다
- 골을 채우던 띠를 걷어냈다. 실선 한 겹뿐이다
- 다 그려지지 않고 조각만 남은 호를 셋 넣었다. 판이 종이에 덜 닿은 자리처럼
- 가운데 점은 진한 통에 가장 옅은 통을 겹쳐 한 단계 가라앉힌다. 가운데 통을 쓰면 파랑에
  노랑이 곱해져 초록이 된다 — 여기서 원하는 것은 더 깊은 파랑이다

### 고침

- **망점 셀보다 가는 선이 점선으로 부서졌다.** 선의 대부분이 가장자리라 스크린이 살려
  두지 않는다. 한 셀은 너끈히 덮을 굵기로 올렸다

### 덤

성기게 만든 것이 마차바퀴까지 없앴다. 고리 셋이면 비율이 어느 박자에서도 0.2 아래다.
방향이 초당 24장에서 48대 0, 여덟 장에서 16대 0으로 완전히 한쪽이 되었다.

## [0.10.0] - 2026-09-16

MOON이 고르개로 돌아왔고, 걸린 판에서 글자를 뺐다.

### 추가

- **MOON.** 판화가 둘이 되면서 고르개와 PLATES 콘택트 시트가 저절로 다시 떴다. 기본은
  RIPPLE 그대로다

### 바뀜

- **걸린 두 판에서 글자를 뺐다.** 움직이는 그림에 얹힌 잔글씨는 매 프레임 읽히기를
  요구하면서 아무것도 말하지 않는다
- 제목을 받는 판이 하나도 안 걸려 있으면 HEADLINE 칸도 함께 숨는다. 어느 판이 제목을
  받는지는 판 자신이 들고 있다

## [0.9.0] - 2026-09-16

느리게 찍을 때 물결이 거꾸로 돌던 것을 고치고, 초당 여덟 장을 기본으로 삼았다.

### 고침

- **물결이 모이는 것처럼 보였다.** 한 걸음에 고리가 나아가는 거리가 고리 사이 간격의
  절반을 넘어, 눈이 바깥 고리 대신 안쪽 고리를 짝지은 것이다. 마차바퀴가 거꾸로 도는 것과
  같다. 초당 열두 장에서는 무늬가 프레임당 33픽셀 안쪽으로 짝지어지고 있었다
- 계마다 고리를 열둘·아홉에서 서넛으로 줄이고 계를 넷으로 늘려 밀도를 되찾았다. 고리마다
  자리와 속도를 흩고, 마루 뒤에 얇은 자취를 한 줄 더했다 — 앞뒤가 다른 모양이면 눈이
  자리가 아니라 모양으로 짝을 짓는다
- **골의 두께를 반지름에 비례시키고 있었다.** 멀리 간 고리가 예순 픽셀짜리 띠가 되어
  물결이 아니라 덩어리로 보였다. 파도 마루는 퍼진다고 굵어지지 않는다. 픽셀로 고정했다

### 바뀜

- 기본이 초당 여덟 장이다

### 재는 법

계 중심에서 바깥으로 밝기를 훑어 이웃 두 프레임을 겹쳐 보고, 어느 쪽으로 밀었을 때 가장
잘 맞는지 찾는다. 세 박자 모두 바깥으로 돌아섰다. 이음매도 다시 재서 0.94~0.99다.

## [0.8.0] - 2026-09-16

초당 장수를 버튼으로 고른다.

### 추가

- **SHEETS A SECOND — 24 · 12 · 8.** 시계는 언제나 초당 스물네 번이고, 바뀌는 것은 한 장을
  몇 프레임 잡아 두느냐다(1·2·3). 48프레임이 셋 모두로 나누어떨어져 한 바퀴가 어디서도
  어긋나지 않는다
- 화면에 걸린 그림이 실제로 초당 24.3 · 12.3 · 8.3번 바뀌는 것을 캔버스 픽셀로 쟀다.
  굽는 시간은 2.0 · 1.1 · 0.8초

## [0.7.0] - 2026-09-16

그림이 초당 열두 장이 되었다. 시계는 초당 스물네 번 그대로다.

### 바뀜

- **한 장을 두 프레임씩 잡아 둔다.** 손그림 애니메이션의 투스 촬영이고, 움직임에 또렷한
  박자가 생긴다. 화면에 걸리는 그림이 실제로 초당 12.5번 바뀌는 것을 캔버스 픽셀로 쟀다
- 굽는 시간이 2.1초에서 1.1초로 줄었다. 찍을 장수가 절반이다
- 박자는 `src/main.js`의 `HOLD` 한 줄이다. 1로 내리면 다시 초당 스물네 장이 된다

## [0.6.0] - 2026-09-16

초당 스물네 장으로 고정. 주소의 `n=` 과 `step=` 은 더 이상 읽지 않는다.

### 바뀜

- **프레임마다 새로 찍는다.** 투스 촬영은 그림이 초당 열두 장이라, 초당 스물네 번이라는
  말과 다르다. 이웃 두 프레임이 서로 다른 판임을 확인했다
- **루프 길이와 촬영 간격 다이얼을 없앴다.** 24fps, 48프레임, 2초. 값이 아니라 약속이다
- 한 바퀴 굽는 데 2.1초. 장수는 두 배가 됐지만 필드 풀이 더워진 뒤라 전보다 빠르다

## [0.5.0] - 2026-09-16

화면이 RIPPLE 하나로 줄었다. `plate=` 를 담은 옛 링크는 어느 판을 가리키든 RIPPLE로 열린다.

### 바뀜

- **걸린 판은 RIPPLE 하나.** 나머지 넷은 `src/plates/`에 그대로 있고 `index.js`에 한 줄씩
  더하면 돌아온다. 판화가 하나뿐이면 고르개가 숨고 그 자리에 이름만 남으며, 판화끼리
  견주는 PLATES 콘택트 시트도 함께 사라진다 — 견줄 것이 없는 자리를 남겨 둘 이유가 없다
- **다이얼을 건드려도 재생이 이어진다.** 움직이는 것이 기본인데 판화를 바꾸면 멈춘 채로
  정지 화면이 남았다. 버튼은 바로 잇고, 슬라이더는 손이 멎은 뒤 잇는다. 한 칸 움직일 때마다
  다시 구우면 아무것도 못 하기 때문이다
- **멈춘 것과 멈춰 달라고 한 것을 나눴다.** STOP을 눌렀거나 프레임을 직접 끌었을 때만 멈춘
  채로 남는다. 그때는 다이얼을 건드려도 저 혼자 다시 돌지 않는다
- 굽는 도중에 다이얼이 또 움직이면 앞의 굽기를 버린다. 세대 번호를 두어 두 벌이 겹쳐
  구워지지 않게 했다

## [0.4.0] - 2026-09-16

판형이 1:1이 되고, 판화가 기본으로 움직인다. 판형과 배치가 함께 바뀌었으므로 같은 롤이
0.3.x와 다른 장을 찍는다.

### 바뀜

- **판형 1000×1000.** 세로 위치를 픽셀로 박아 두었던 자리를 `height`의 비율로 돌렸다.
  다음번에 판형을 바꿔도 다섯 장이 한꺼번에 무너지지 않는다
- **판화가 기본으로 움직인다.** 다섯 장 모두 `t`를 쓴다. POSTER는 숨 쉬고 곁가지가 깜박이며,
  MEDIUM은 침이 계조를 훑고, MOON은 달이 오르내리고 별이 반짝이며, GARDEN은 바람에 흔들린다
- **열면 바로 재생된다.** 콘택트 시트를 펼친 채로 들어왔거나 쓰는 사람이 움직임을 줄여 달라고
  해 둔 경우는 그러지 않는다
- 캔버스가 두 변 모두에 맞춰 줄어든다. 높이를 1200px로 박아 두었던 탓에 정사각으로 바꾸는
  순간 화면 밖으로 넘쳐 나갔다

### 고침

- **MOON의 루프가 튀었다.** 이음매 걸음이 보통 걸음의 4배였다. 두 가지가 겹쳐 있었다.
  물빛 흔들림의 폭을 `t`에 비례시켜 한 바퀴 끝에서 제자리로 튀게 만든 것이 하나고, 별을
  건너뛸지를 달의 위치로 정한 것이 다른 하나다. 달이 흔들리면 걸러지는 별이 달라지고 그 뒤
  별이 전부 다른 난수를 받아 하늘이 통째로 갈렸다. 시간이 난수 흐름을 건드리면 안 된다
- `wave`가 위상을 받는다. 물결이 선 위를 흘러간다

## [0.3.0] - 2026-09-16

인쇄기가 시간을 알아듣는다. 얼룩 필드의 해상도가 바뀌어 같은 롤이 0.2.x와 미세하게 다른
장을 찍는다.

### 추가

- **움직이는 판화.** `page.t`가 0에서 1로 한 바퀴 돈다. `t`를 안 보는 판화는 그대로 정지된
  한 장이다. 이어지는 조건은 하나 — 판화 안에서 시간을 쓰는 모든 것이 `t`에 대해 정확히 한
  바퀴여야 한다
- **RIPPLE.** 24fps 물결. 목판의 물결은 선이 아니라 폭이 변하는 띠라, 고리 두 개 사이를
  evenodd로 채우고 마루보다 조금 내려 앉혀 빛이 위에서 오게 했다
- **필름 굽기와 재생.** 한 프레임을 찍는 데 재생 예산보다 오래 걸리므로 미리 찍어 두고 튼다.
  다이얼을 건드리면 버리고 다시 굽고, 안 건드렸으면 즉시 다시 튼다
- **투스 촬영.** 두 프레임에 한 장. 손그림 애니메이션의 기본이고 찍을 장수가 절반이 된다
- **끓음(BOIL).** 종이가 그림과 함께 움직이는가, 그림이 종이 밑에서 움직이는가. 말로 정할
  수 없는 물음이라 HELD·TWOS·EVERY 다이얼로 두었다. 인상이 바뀌면 스크린과 판 어긋남이
  함께 바뀐다
- **콘택트 시트 FRAMES.** 한 판화를 한 바퀴에 걸쳐 펼친다

### 바뀜

- `printSheet`이 종이 크기 대신 배율을 받는다. 판화는 언제나 900×1200 좌표로 그리고 배율은
  모른다. 축소판마다 여백과 글자 크기가 따로 놀던 것이 사라지고, 콘택트 시트도 축소가 아니라
  처음부터 작게 찍는다 — 아홉 장이 590 ms에서 170 ms로
- 얼룩 필드를 4픽셀에 한 번만 잰다. 주기가 110픽셀과 31픽셀이라 망점 아래에서 차이가 보이지
  않는데 값은 열여섯 배 덜 만든다

### 고침

- 굽는 동안 `requestAnimationFrame`으로 창을 놓아주고 있었다. 창이 앞에 없으면 rAF는 초당
  한 번까지 조여져 2초짜리 루프를 굽는 데 45초가 걸렸다. 굽는 일은 화면에 그리는 일이 아니다
- 필름을 다시 구우면서 옛 벌의 비트맵(80 MB)을 놓아주지 않았다. 다이얼 몇 번에 수백 MB가
  쌓이고, 그때부터는 인쇄가 느려진 것처럼 보였다
- 필드 풀이 좁아 끓음 한 바퀴를 도는 동안 쓸 것을 저희끼리 밀어냈다

## [0.2.0] - 2026-09-16

인쇄기를 다시 짰다. 같은 롤이 0.1.x와 다른 장을 찍는다. 0.x에서는 MINOR가 그 자리를
맡으므로 여기서 올린다.

### 바뀜

- 잉크가 태그가 아니라 **그릴 수 있는 분판**이 되었다. 마크마다 번호를 붙여 목록에 쌓고
  통마다 그 목록을 훑으며 제 것을 고르던 것을, `S.key.line(...)` 한 줄로 바꿨다. 판을 짜는
  쪽에서 그게 무슨 색으로 나올지 이제 코드만 봐도 안다
- 스크린이 확률적 점에서 **망점**으로 바뀌었다. 통마다 각도가 45°/15°/75°로 다르고, 겹친
  자리에 로제트가 뜬다. 드럼은 종이를 100% 덮지 못하므로 꽉 찬 면에도 바늘구멍이 남는다
- 구성이 코드 한 덩어리가 아니라 **이름 붙은 판화의 목록**이 되었다. 한 장이 함수 하나다

### 추가

- 판화 넷 — POSTER, MEDIUM(스크린을 판단하는 자리), MOON(계조와 녹아웃), GARDEN(겹침)
- 콘택트 시트. PLATES는 같은 롤로 모든 판화를, INKS는 같은 판화를 배색 아홉 벌로 펼친다.
  한 장만 보고서는 알 수 없는 것 — 이 배색에서 글자가 읽히는지 같은 것 — 을 여기서 본다
- CELL 다이얼. GRAIN을 0으로 내리면 얼룩 없는 깨끗한 망점이 되어 스크린만 따로 판단할 수 있다
- 톤 그라데이션(`ramp`)과 녹아웃(`knockout`)이 분판의 기본 동작이 되었다

### 고침

- 잎이 타원이었다. 양 끝에서 폭과 기울기가 함께 0이 되도록 고쳐 꼭지점이 생겼다
- 서버가 `../`로 올라가는 모듈 경로에는 부팅 도장을 찍지 않았다. 판화가 그 경로를 쓴다

### 없앰

- `src/grain.js`와 `src/poster.js`. 각각 `src/screen.js`와 `src/plates/`로 갈라졌다

## [0.1.1] - 2026-09-16

### 고침

- 포트가 이미 쓰이는 중일 때 서버가 처리되지 않은 오류로 스택을 쏟았다. 무슨 일인지 한 줄로
  알리고 쓸 수 있는 다른 포트를 일러준 뒤 1로 끝낸다

## [0.1.0] - 2026-09-16

첫 판.

### 추가

- 드럼별로 종이를 한 번씩 통과시키는 인쇄 파이프라인. 마스크, 그레인 스크린, 잉크,
  어긋난 자리에 곱하기
- 잉크 배색 아홉 벌과 2도·3도 전환
- 유기적 덩어리와 멤피스 장식으로 짜는 배치, 롤 하나에 한 장
- 다이얼 여섯 개와 PNG 내보내기, 설정 전체를 담는 주소
- 모듈 URL에 부팅 도장을 찍는 정적 서버

[0.20.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.20.0
[0.19.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.19.0
[0.18.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.18.0
[0.17.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.17.0
[0.16.1]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.16.1
[0.16.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.16.0
[0.15.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.15.0
[0.14.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.14.0
[0.13.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.13.0
[0.12.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.12.0
[0.11.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.11.0
[0.10.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.10.0
[0.9.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.9.0
[0.8.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.8.0
[0.7.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.7.0
[0.6.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.6.0
[0.5.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.5.0
[0.4.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.4.0
[0.3.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.3.0
[0.2.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.2.0
[0.1.1]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.1.1
[0.1.0]: https://github.com/tomlim2/riso-graphic/releases/tag/v0.1.0
